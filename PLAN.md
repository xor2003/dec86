1. [done] `/home/xor/vextest/meta_harness/orchestrator.py:57-65,394-429`, `/home/xor/vextest/meta_harness/cli.py:7,39-69`, `/home/xor/vextest/meta_harness/tests/test_orchestrator.py:832-848`, `/home/xor/vextest/meta_harness/tests/test_cli.py:122-147`: distinguish preflight resource blocks from crashes by raising a dedicated blocked error, recording `blocked-low-disk`/`blocked-low-ram` in cycle state before exit, and returning a blocked CLI exit without crash-review. Functions/tests/scripts: `ResourceBlockedError`, `preflight_resource_check`, `main`, `test_preflight_resource_check_marks_low_disk_as_blocked`, `test_cli_main_treats_resource_block_as_blocked`. Done when low-resource preflight no longer falls into the generic crash-review path, the cycle state keeps the blocked step, and `pytest -q /home/xor/vextest/meta_harness/tests/test_cli.py /home/xor/vextest/meta_harness/tests/test_orchestrator.py -k 'resource_block or planner_timeout or sigterm_exit or finalize_run or low_disk_as_blocked or done_with_failures'` passes.
2. [done] `/home/xor/vextest/decompile.py`, `/home/xor/vextest/AGENTS.md`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: make failure handling deterministic and honest by reporting lift-break locations, emitting non-optimized C fallback before asm when decompilation fails or times out, and recording those fallback rules in agent guidance. Functions/tests/scripts: `_probe_lift_break`, `_try_decompile_non_optimized_slice`, `_infer_linear_disassembly_window`, `test_probe_lift_break_reports_first_bad_instruction`, `test_try_decompile_non_optimized_slice_uses_raw_slice`. Done when direct and whole-binary fallback paths print a problem marker plus either non-optimized C or widened asm, and `pytest -q /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'probe_lift_break or non_optimized_slice'` passes.
3. [done] `/home/xor/vextest/decompile.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: strengthen no-helper recovery with fast seed heuristics based on call targets, classic prologues, and epilog-follow-on starts, while explicitly reporting the raw-recovery lane in CLI output. Functions/tests/scripts: `_rank_exe_function_seeds`, `_recover_fast_seed_functions`, `test_rank_exe_function_seeds_uses_epilog_follow_ons`. Done when helper-free binaries say helper metadata is absent, raw recovery continues, and the seed ranker covers epilog-follow-on starts in tests.
4. [done] `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/cod_extract.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/lst_extract.py`, `/home/xor/vextest/decompile.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: let sibling `.COD` listings contribute automatic function labels, ranges, proc kinds, and per-function source/proc metadata for `.EXE` runs. Functions/tests/scripts: `CODListingMetadata`, `extract_cod_listing_metadata`, `_parse_cod_sidecar_metadata`, `_sidecar_cod_metadata_for_function`, `test_extract_cod_listing_metadata_reads_life_cod_ranges`, `test_sidecar_cod_metadata_for_function_uses_sibling_cod`. Done when `LIFE.EXE` sidecar loading includes `cod_listing` and function-level COD source lines are available without `--proc`.
5. [done] `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/flair_extract.py`, `/home/xor/vextest/decompile.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add first-pass FLAIR `.pat`/`.sig` support by matching startup patterns against entry bytes and reading local signature-catalog metadata via `dumpsig`. Functions/tests/scripts: `match_flair_startup_entry`, `list_flair_sig_libraries`, `_detect_flair_metadata`, `test_match_flair_startup_entry_matches_watcom_startup_pattern`, `test_list_flair_sig_libraries_reads_pascal_catalogs`. Done when helper metadata loading can report `flair_pat`/`flair_sig` sources and the startup-pattern/signature-catalog tests pass.
6. [done] `/home/xor/vextest/omf_pat.py`, `/home/xor/vextest/decompile.py`, `/home/xor/vextest/AGENTS.md`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add a deterministic local OMF-to-PAT lane in the decompiler root that first uses local `plb` for `.obj/.lib`, falls back to repo-owned OMF `.obj`/`.lib` PAT generation when `plb` yields nothing, parses Microsoft `F0` library headers plus `F2` extended dictionaries, and consumes those PATs as sidecar evidence during FLAIR detection. Functions/tests/scripts: `ensure_pat_from_omf_input`, `generate_pat_from_omf_obj`, `generate_pat_from_omf_lib`, `parse_microsoft_lib`, `match_pat_modules`, `discover_local_pat_matches`, `_detect_flair_metadata`, `test_ensure_pat_from_omf_input_generates_fallback_pat_for_obj`, `test_extract_omf_modules_from_lib_reads_page_aligned_module`, `test_parse_microsoft_lib_reads_header_and_extended_dictionary`, `test_match_pat_modules_labels_unique_generated_function_match`, `test_detect_flair_metadata_merges_local_pat_matches`. Done when real OMF objects such as `reko/subjects/OBJ-msdos-x86/em86.obj` and Microsoft-style libraries yield PAT modules locally, local PAT matches can contribute code labels/ranges, and `pytest -q /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'extract_omf_modules_from_lib or parse_microsoft_lib or generate_pat_from_omf_lib or ensure_pat_from_omf_input or match_pat_modules or detect_flair_metadata_merges_local_pat_matches or flair_sig_libraries or match_flair_startup_entry'` passes.
