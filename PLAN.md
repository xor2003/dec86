1. Goal: Reduce bounded slice-recovery cost for `LIFE2.EXE` `sub_119d3` so at least one non-optimized attempt can complete before the string fallback on the direct `--addr` path.
Why now: `/tmp/life2.sub119d3.addr.out` shows the new per-attempt bound works, but all three attempts still end as `lean bounded attempt: TimeoutError`, `full-no-refs bounded attempt: TimeoutError`, and `full-with-refs bounded attempt: TimeoutError` before string fallback. The old blind spot is gone; the remaining issue is slice-recovery cost inside `_pick_function_lean()` / `_pick_function()`.
Edit targets: `/home/xor/vextest/inertia_decompiler/slice_recovery.py`; one new small helper under `/home/xor/vextest/inertia_decompiler/` if needed; `/home/xor/vextest/inertia_decompiler/cli.py` only for narrow wiring; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`.
Required edits: profile or trim the bounded slice attempts without adding sample-specific logic; prefer changing bounded attempt composition, preconditions, or cost guards over another timeout bump; keep the first structured failure detail and partial-output preference intact.
Required tests: keep the new bounded-attempt regressions in `test_x86_16_cli.py`; add one focused test for the chosen cost-control rule if a new helper is introduced.
Verification commands: `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_cli.py -k "non_optimized_slice or partial_timeout or string_intrinsic"`; `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x119d3 --timeout 5 > /tmp/life2.plan2.out 2>&1`; `rg -n "non-optimized fallback unavailable|string intrinsic fallback|shared-project slice|fresh-project slice|sub_119d3" /tmp/life2.plan2.out`.
Definition of done: the direct `--addr 0x119d3` repro either emits bounded non-optimized C/partial C or it fails with a sharper lower-layer reason than three generic bounded-attempt timeouts.
Stop conditions: stop if the hot path is below slice recovery and belongs in lift/CFG work; stop if the only apparent win is another timeout increase without a real cost change.
