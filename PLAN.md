7. [pending] `/home/xor/vextest/decompile.py:2585-2778,14046-14091`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: codify the `LIFE.EXE` vs `LIFE2.EXE` gap as a deterministic stripped-binary regression by adding a comparison helper/test that proves `LIFE.EXE` currently gets a sidecar-backed catalog while `LIFE2.EXE` falls back to the raw quick-scan lane and recovers only the startup cluster. Functions/tests/scripts: `_rank_exe_function_seeds`, `_recover_fast_seed_functions`, `_fallback_entry_function`, new `test_life2_without_helper_metadata_stays_in_raw_quick_scan_lane`, and the direct CLI probes `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE.EXE --timeout 6 --max-functions 12` plus `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6 --max-functions 12`. Done when the regression captures the current evidence delta explicitly: `LIFE.EXE` reports sidecar metadata and a large known-function catalog, while `LIFE2.EXE` is classified as helper-free and names only the small startup-adjacent set.
8. [pending] `/home/xor/vextest/decompile.py:585-730,2585-2778,1495-1563`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add a deterministic peer-binary evidence lane that can borrow function starts/ranges/names from a nearby sidecar-backed sibling executable only when code bytes match exactly at the candidate function entry and across the claimed function span, recording that provenance separately from `.cod/.lst/.map` metadata. Functions/tests/scripts: `_detect_flair_metadata`, `_rank_exe_function_seeds`, `_pick_function`, new peer helpers such as `_discover_peer_exe_catalog_matches` / `_exact_function_span_matches` / `_merge_peer_function_catalog`, and tests `test_peer_exe_catalog_requires_exact_span_byte_match`, `test_detect_flair_metadata_merges_peer_exe_matches`, `test_life2_reuses_life_function_catalog_when_spans_are_identical`. Done when `LIFE2.EXE` can import exact byte-identical function labels/ranges from `LIFE.EXE` with an explicit `peer_exe` source marker, while any non-identical span is rejected and left in the raw recovery lane.
9. [pending] `/home/xor/vextest/decompile.py:2739-2795,1316-1423`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/README.md`: consume the peer-derived catalog at the discovery layer by feeding imported starts into the fast EXE catalog and cache/reporting paths, then document the new evidence source and verify the `LIFE2.EXE` improvement against `LIFE.EXE`. Functions/tests/scripts: `_recover_fast_seed_functions`, `_recover_fast_exe_catalog`, `_build_project`, new `test_life2_peer_catalog_recovers_main_and_init_functions`, and the smoke command `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6 --max-functions 20`. Done when `LIFE2.EXE` deterministically reports `peer_exe` helper metadata, recovers `main`, `init_life`, `init_buf`, `draw_box`, and `init_mats` by name in the shown catalog, and the README briefly explains that exact byte-identical sibling binaries may supply function catalogs when native sidecars are absent.
