follow architecturally AGENTS.md. remove done steps only when corpus DoD really hit.

0. Goal: Fix the current blocking crash path before more semantic recovery work lands.
Why now: loop recovery and later structured cleanup cannot be trusted while the current `QuickSort`/similar path can still fail on `Non-constant VexValue has no value property`-style recovery breaks. AGENTS.md requires solving blockers before layering more rewrite logic on top. Current quality: correctness red.
Edit targets: the earliest failing path localized by focused repro, expected under `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/`, and focused regressions under `/home/xor/vextest/angr_platforms/tests/`.
Required edits: localize the first bad layer that lets a non-constant `VexValue` escape into direct-value access; fix it there without adding text-based fallbacks; keep emitted failure attribution honest if a separate unresolved path remains.
Required tests: add or extend one focused crash regression for the failing function family and keep the existing `PercolateUp` no-crash anchor passing.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "PercolateUp or QuickSort or VexValue"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10ce0 --timeout 30 --alternate-source-c`.
Definition of done: the focused crash repro no longer fails in the VexValue path, and later semantic passes run on a real recovered function instead of dying before they start.
Stop conditions: stop if the only candidate fix is to swallow the exception and pretend success; fix the bad semantic layer or keep the failure explicit.

1. Goal: Make whole-tail validation a hard gate for every semantic change in this execution lane.
Why now: AGENTS.md says validation must remain surfaced honestly. Semantic rewrites are not acceptable if they only improve text while the before/after tail boundary still reports `changed`. Current quality: correctness red when ungated.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_structuring_stage.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py`; focused validation tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: keep tail validation enabled for the focused one-function lane; ensure each semantic pass captures before/after whole-tail summaries and does not silently accept a `changed` delta; preserve `changed`, `unknown`, and `uncollected` identities instead of collapsing them into success.
Required tests: keep focused tail-validation and structuring/postprocess validation tests passing; add one regression if a pass currently mutates semantics without a live-out check.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_tail_validation.py angr_platforms/tests/test_x86_16_structuring_pass_validation.py`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f38 --timeout 30 --alternate-source-c`.
Definition of done: focused semantic work in this lane runs with validation on, and no item below is considered done if its focused result is still `changed`.
Stop conditions: stop if the only way to pass is to disable validation or loosen verdicts; fix the semantic mismatch instead.

2. Goal: Finish Condition IR as the primary branch meaning and stop reconstructing live branch semantics from raw flags late.
Why now: AGENTS.md requires ALU and branch semantics to survive as typed conditions, with late rewrite only as cleanup. The remaining `SORTDEMO` debt is not “make Sleep prettier”; it is “make final branch nodes independent of raw `flags_*` reasoning.” Current quality: correctness red, readability red.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/alu_helpers.py:1-120`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/condition_ir.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/ir/vex_condition_lifting.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_flags.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: have ALU helpers emit typed compare/condition meaning directly; keep CFG/structuring consumers on `Condition` instead of raw flag-mask formulas; allow postprocess to clean or normalize already-typed conditions only as a fallback consumer, not as the main source of branch meaning; do not reconstruct final branch semantics from raw flags late if earlier typed facts exist.
Required tests: keep signed, unsigned, eq, ne, and zero/nonzero cases covered; add or tighten one assertion that final focused branch condition trees do not contain raw `flags_*` dependencies when typed conditions exist.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_condition_ir.py angr_platforms/tests/test_x86_16_decompiler_postprocess_flags.py angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "Sleep or PercolateUp"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f38 --timeout 30 --alternate-source-c`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x109e8 --timeout 30 --alternate-source-c`.
Definition of done: focused `Sleep`/`PercolateUp` outputs have no raw `flags_*` branch syntax, and the focused branch condition trees no longer depend on flag variables where typed `Condition` facts exist; focused validation is not `changed`.
Stop conditions: stop if the only available fix is late flag-pattern rewriting over rendered C/asm text; strengthen the earlier ALU/Condition layer instead.

3. Goal: Preserve ALU semantics so live expressions never degrade into `...` placeholders before codegen.
Why now: `SORTDEMO.dec:151` and `SORTDEMO.dec:2068` still show `flags_2 = ...;`. That is semantic loss, not formatting. AGENTS.md requires ugly but truthful output, not dropped meaning. Current quality: correctness red, recompilation red.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/alu_helpers.py:1-120`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/condition_ir.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/ir/vex_condition_lifting.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_flags.py`; `/home/xor/vextest/inertia_decompiler/decompilation_quality.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: carry add/sub carry, borrow, overflow, and widened-result evidence as typed value/condition artifacts instead of letting them collapse into renderer holes; make later passes consume those typed facts before printing flag temporaries; treat emitted `...` as a real unresolved semantic error in quality gating, not as acceptable output.
Required tests: add one focused arithmetic-carry regression that currently emits `...`; keep existing condition/flag tests passing.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_condition_ir.py angr_platforms/tests/test_x86_16_decompiler_postprocess_flags.py angr_platforms/tests/test_x86_16_cli.py -k "ellipsis or carry or overflow or Sleep"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f38 --timeout 30 --alternate-source-c`.
Definition of done: focused `SORTDEMO` anchors emit no `...`; unresolved arithmetic semantics use an explicit typed refusal/fallback rather than an empty placeholder expression.
Stop conditions: stop if the only candidate fix is substituting text for `...`; add the missing typed ALU artifact instead.

4. Goal: Canonicalize stable indexed stack references into exact stack slots before emitted C cleanup.
Why now: `Sleep` showed that `Reference(CIndexedVariable(Reference(stack_var), const))` was surviving long enough to print `(&s_6)[8]` and `*(&slot)` even when exact slot identity was already recoverable. This is the first, smallest executable slice of the broader stack-identity debt. Current quality: readability red but improving.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli_stack_cvars.py`; `/home/xor/vextest/inertia_decompiler/cli.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: canonicalize typed indexed stack references and dereference-of-reference forms from stack-slot identity rather than from rendered text; keep the rewrite limited to cases with one exact stable slot.
Required tests: keep focused stack-cvar matcher and canonicalization tests passing; keep the focused `Sleep` one-function regression passing.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cli_stack_matchers.py angr_platforms/tests/test_x86_16_cod_regressions.py -k "stack_cvar or indexed_stack"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f28 --timeout 30 --alternate-source-c`.
Definition of done: focused `Sleep` output no longer contains `(&s_6)[N]` or `*(&slot)` where one exact stack slot identity is already stable; focused validation is not `changed`.
Stop conditions: stop if the only available proof is a rendered-text pattern; use typed stack-slot identity only.

5. Goal: Materialize stable placeholder-backed stack locals before late cleanup so `stack_bp_*` aliases stop leaking into emitted C.
Why now: `PercolateUp` still emits `stack_bp_m6_b1`, `(&stack_bp_m6_b1)[8]`, and related placeholder indexing. This is a different failure from `Sleep`: the stack base itself is still unresolved before the final text-normalization lane. Current quality: readability red, recompilation red.
Edit targets: the earliest stack identity layer, expected in `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/` plus only the minimal supporting changes in `/home/xor/vextest/inertia_decompiler/cli_stack_locals.py`, `/home/xor/vextest/inertia_decompiler/cli_stack_cvars.py`, `/home/xor/vextest/inertia_decompiler/cli.py`, and `/home/xor/vextest/inertia_decompiler/cli_local_rewrites.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: build stable `_StackSlotIdentity` or equivalent typed stack object for placeholder-backed stack bases before emitted C cleanup; make stack matcher/address classification consume typed indexed stack views instead of losing them to placeholder text; preserve explicit uncertainty when slots overlap instead of inventing alias names late.
Required tests: add one focused `PercolateUp` placeholder-stack regression and keep existing stack-cvar/stack-byte-offset tests passing.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cli_stack_matchers.py angr_platforms/tests/test_x86_16_cli.py -k "PercolateUp or stack_cvar or stack_byte"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x109e8 --timeout 30 --alternate-source-c`.
Definition of done: focused `PercolateUp` output no longer contains `stack_bp_*` or `(&stack_bp_*)[N]` when the slot identity is stable; unresolved overlaps remain explicit rather than guessed.
Stop conditions: stop if the only way to rename the slot is via rendered C text cleanup; add the earlier typed stack-slot artifact first.

6. Goal: Make covering-slot resolution prefer stable word/argument slots over byte-index aliases.
Why now: even after exact indexed references are canonicalized, covering-slot resolution can still leak byte-sized stack bases into word-sized accesses or prefer unstable aliases over known argument/word slots. Current quality: correctness amber, readability red.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli_stack_cvars.py`; `/home/xor/vextest/inertia_decompiler/cli_stack_byte_offsets.py`; `/home/xor/vextest/inertia_decompiler/cli_stack_coalesce.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: make covering-slot resolution refuse byte-index aliases when one stable word slot or argument slot already exists; keep overlap handling conservative and explicit.
Required tests: add one focused `Sleep`/`PercolateUp` covering-slot regression and one refusal case for overlapping stack slots.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cli_stack_byte_offsets.py angr_platforms/tests/test_x86_16_cli.py -k "stack_cvar or stack_byte or Sleep or PercolateUp"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f28 --timeout 30 --alternate-source-c`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x109e8 --timeout 30 --alternate-source-c`.
Definition of done: focused `Sleep`/`PercolateUp` outputs no longer degrade stable word/argument slots into byte-index stack aliases; unresolved overlaps remain explicit rather than guessed.
Stop conditions: stop if the only proof of a covering slot is adjacent rendered C text; use typed storage identity/range facts instead.

7. Goal: Preserve 16-bit store width from IR/alias facts so stable word stores do not degrade into paired byte writes.
Why now: `SORTDEMO.dec:81-82`, `SORTDEMO.dec:261-262`, `SORTDEMO.dec:427-432`, `SORTDEMO.dec:527-530`, and many later rows still emit `*((char *)...)=ax; *((char *)...+1)=ax>>8;`. This is an alias/widening failure. Width must come from typed memory effects, not be guessed late from adjacent byte stores. Current quality: correctness amber, readability red, recompilation red.
Edit targets: the earliest store-width/alias layer plus only the necessary consumers in `/home/xor/vextest/inertia_decompiler/cli_word_loads.py:48-158`, `/home/xor/vextest/inertia_decompiler/cli_segmented_store_coalesce.py`, `/home/xor/vextest/inertia_decompiler/cli_stack_coalesce.py:6-63`, and `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_globals.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: preserve 16-bit write width in typed store facts; widen adjacent byte-store pairs only after stable alias proof on the destination storage object; keep segment/base/offset identity attached while joining; unify the stable-word-store path for SS locals and DS globals; refuse ambiguous mixed-source joins instead of printing guessed word stores.
Required tests: add one DS-global word-store regression, one SS-local word-store regression, and one refusal case for non-adjacent or conflicting byte stores.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cod_regressions.py angr_platforms/tests/test_x86_16_cli.py -k "word_store or byte_pair or segmented_store"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10010 --timeout 30 --alternate-source-c`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x109e8 --timeout 30 --alternate-source-c`.
Definition of done: focused `SORTDEMO` anchors stop printing paired byte writes for stable 16-bit stores and instead emit one typed word store or one stable global/stack object assignment; ambiguous cases still refuse to widen; focused validation is not `changed`.
Stop conditions: stop if the only proof of a 16-bit store is adjacent rendered C text; expose the earlier store-width fact first.

8. Goal: Recover induction loops only after the current crash and semantic blockers are cleared.
Why now: the induction direction is correct, but it sits behind the crash path, Condition IR debt, stack identity debt, and store-width debt. AGENTS.md requires solving earlier semantic blockers first. Current quality: readability red, recompilation red until prerequisites are met.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli_access_object_hints.py`; `/home/xor/vextest/inertia_decompiler/cli_access_profiles.py`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/type_array_matching.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: keep induction summaries as evidence artifacts; reuse existing `AccessTraitStrideEvidence` / `AccessTraitEvidenceProfile`; drive `_rewrite_induction_loops_8616(codegen)` only from stable evidence; do not spend more effort here until items 0-5 are not blocking the focused functions.
Required tests: `angr_platforms/tests/test_x86_16_induction_loops.py`; `angr_platforms/tests/test_x86_16_induction_summaries.py`; and focused `SORTDEMO` anchors for `HeapSort`, `PercolateUp`, and `QuickSort`.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_induction_loops.py angr_platforms/tests/test_x86_16_induction_summaries.py angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "HeapSort or QuickSort or PercolateUp"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10970 --timeout 30 --alternate-source-c`.
Definition of done: at least one real corpus loop stays recovered from induction evidence after the earlier semantic fixes, ambiguous evidence still refuses to rewrite, and focused validation is not `changed`.
Stop conditions: stop if loop recovery is being used to paper over unresolved crash/condition/alias/store-width debt.

9. Goal: Lower stable DS/SS segmented accesses into named globals/arrays/objects only after segment identity survives to the object layer.
Why now: `SORTDEMO.dec:81-82`, `SORTDEMO.dec:259-265`, `SORTDEMO.dec:422-432`, and many later rows still print raw `ds << 4` / `ss << 4` arithmetic even where stable globals such as `cRow`, `iCompares`, and `abarWork` should be recoverable. The fix must keep segment identity attached until object lowering, not flatten first and guess later. Current quality: readability red, recompilation red.
Edit targets: the typed address/storage path in `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/segmented_memory_reasoning.py` and `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/type_storage_object_bridge.py`, plus only the minimal consumers in `/home/xor/vextest/inertia_decompiler/cli_segmented.py`, `/home/xor/vextest/inertia_decompiler/cli_segmented_lowering.py`, and `/home/xor/vextest/inertia_decompiler/cli_access_object_hints.py`; focused tests under `/home/xor/vextest/angr_platforms/tests/`; `/home/xor/vextest/PLAN.md`.
Required edits: push stable segment-space association into typed address/storage objects before CLI text cleanup; let object/array lowering consume those typed addresses for globals and repeated indexed arrays; keep DS/SS distinct until lowering proof is strong; lower to named globals/arrays only when evidence is stable and keep raw segmented arithmetic for ambiguous cases.
Required tests: add one `cRow`/global word anchor, one `abarWork[index]` array anchor, and one refusal case where segment identity remains ambiguous.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_segmented_memory.py angr_platforms/tests/test_x86_16_storage_objects.py angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "segment or storage object or HeapSort or PercolateUp or main"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10010 --timeout 30 --alternate-source-c`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10970 --timeout 30 --alternate-source-c`.
Definition of done: focused `SORTDEMO` output lowers stable accesses like `cRow`, `iCompares`, and `abarWork[i]` through the normal typed object path and materially reduces raw `ds << 4` / `ss << 4` arithmetic without flattening ambiguous segment cases; focused validation is not `changed`.
Stop conditions: stop if the only way to name the object is symbol-name substitution without typed address/storage proof; strengthen the segment/object evidence layer first.

Global DoD: focused acceptance cases have no raw terminating `flags_*` branch syntax in the targeted compare/loop anchors, at least one major real corpus loop is recovered from induction evidence, whole-tail validation remains surfaced honestly, and finished items stay removed from this checklist only after the focused corpus confirms their DoD.
