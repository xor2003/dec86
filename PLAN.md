1. Finish `Condition` IR and stop emitting raw flag semantics as the primary branch meaning. Target files: `angr_platforms/angr_platforms/X86_16/alu_helpers.py:1-90`, new `angr_platforms/angr_platforms/X86_16/condition_ir.py`, `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_flags.py`, and `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`. Concrete edits: extend `build_compare_condition_8616(...)` to emit typed ops for `eq`, `ne`, signed/unsigned ordering, and zero/nonzero; have `compare_operation(...)` record typed branch meaning; consume typed conditions first in flag postprocess; run condition lowering before boolean-CITE cleanup. Tests: new `angr_platforms/tests/test_x86_16_condition_ir.py`, `angr_platforms/tests/test_x86_16_decompiler_postprocess_flags.py`, and a focused `SORTDEMO.EXE::Sleep` corpus regression. DoD: signed, unsigned, equality, and nonzero cases are covered; `Sleep` no longer terminates on raw `flags_*` predicates; tail validation is not `changed`.

2. Preserve typed `Address` semantics and stop leaking `(seg << 4) + off` as recovered meaning. Target files: `angr_platforms/angr_platforms/X86_16/addressing_helpers.py`, `angr_platforms/angr_platforms/X86_16/access.py`, `angr_platforms/angr_platforms/X86_16/alias_model.py`, and new `angr_platforms/angr_platforms/X86_16/address_ir.py`. Concrete edits: add `IRAddress(segment, offset, width_bits, address_bits)`; keep `ResolvedMemoryOperand` as the bridge object but make `linear` backend-only; preserve SS/DS/ES explicitly through load/store recovery; feed stable SS facts into aliasing without using final C shape as proof. Tests: new `angr_platforms/tests/test_x86_16_address_ir.py` and `angr_platforms/tests/test_x86_16_segment_address_model.py`. DoD: SS/DS/ES remain distinct in the typed model and SS stack candidates can be derived without relying on rendered-expression shape.

3. Use alias facts to recover stack slots, not just classify them. Target files: `angr_platforms/angr_platforms/X86_16/alias_model.py`, `angr_platforms/angr_platforms/X86_16/decompiler_structuring_stage.py`, and `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`. Concrete edits: lower stable typed SS addresses into `_StackSlotIdentity` / `_StorageDomainSignature("stack", ...)`; extend `_stack_storage_facts_for_segmented_address_8616(...)` into the decompiler pipeline; add `_lower_stable_ss_stack_accesses_8616(codegen)` in structuring or early postprocess; refuse lowering when segment/offset/width evidence is weak. Tests: `angr_platforms/tests/test_x86_16_segmented_stack_alias.py` plus a focused `SORTDEMO.EXE::main` corpus regression. DoD: `main` shows fewer explicit `((ss << 4) + ...)` accesses, DS/ES accesses are not mis-lowered, and tail validation is not `changed`.

4. Recover loop and induction semantics from existing stride evidence. Target files: `inertia_decompiler/cli_access_object_hints.py`, the access-trait module that owns `AccessTraitStrideEvidence` / `AccessTraitEvidenceProfile`, and `angr_platforms/angr_platforms/X86_16/decompiler_structuring_stage.py`. Concrete edits: add an induction summary type such as `InductionSummary(base_key, index_key, stride, direction, bound_candidate)`; reuse the existing stride/induction evidence instead of inventing a new side channel; add `_rewrite_induction_loops_8616(codegen)` for stable `while(true)+break` patterns. Tests: `angr_platforms/tests/test_x86_16_induction_loops.py` with focused corpus anchors for `SORTDEMO.EXE::PercolateUp` and `SORTDEMO.EXE::QuickSort`. DoD: at least one real corpus loop becomes a structured bounded loop; ambiguous evidence refuses to rewrite; no tail-validation regressions on the converted functions.

5. Normalize adjacent byte slices into wider values only after alias proof. Target files: `angr_platforms/angr_platforms/X86_16/alias_domains.py`, `angr_platforms/angr_platforms/X86_16/alias_transfer.py`, `angr_platforms/angr_platforms/X86_16/alias_state.py`, and `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_simplify.py`. Concrete edits: drive widening through `View.can_join`, `join_register_views`, and `RegisterConcatExpr`; fold adjacent low/high byte joins and byte-pair-to-word patterns only when domain + view evidence agrees; refuse mixed-domain or incompatible-view folds. Tests: `angr_platforms/tests/test_x86_16_alias_widening.py`. DoD: AL/AH to AX and adjacent stack-byte-to-word folds happen only with alias proof; negative cases refuse; output noise from byte-pair expressions is reduced on one focused corpus case.

6. Wire whole-tail validation through every semantic pass touched above, and preserve status identities end-to-end. Target files: `angr_platforms/angr_platforms/X86_16/tail_validation.py:639-706, 1332-1442`, `angr_platforms/angr_platforms/X86_16/decompiler_structuring_stage.py`, `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`, and the recovery driver that reports validation state. Concrete edits: capture validation before and after condition lowering, stack lowering, loop rewriting, and callsite summary application; keep `changed`, `unknown`, and `uncollected` distinct; do not soften a non-success into a pass; surface validation in corpus regressions. Tests: extend the focused corpus tests so they assert verdict state explicitly, plus the existing `angr_platforms/tests/test_x86_16_tail_validation.py`. DoD: every new semantic pass has a validation assertion, and no item closes without either a passing verdict or an explicit preserved non-success identity.

Global DoD: final C for the acceptance set contains no raw `flags_*` predicates in terminating branches, stable SS accesses are lowered to stack storage instead of explicit segment arithmetic, at least one major corpus loop is recovered from real induction evidence, widening happens only with alias proof and negative tests, callsites are improved conservatively, tail validation is exercised on every touched semantic pass, and the acceptance lane still decompiles without asm fallback while reporting any remaining uncertainty honestly.
