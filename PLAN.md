1. Goal: Decide the honest `LIFE2.EXE` sweep-lane policy for `sub_119d3` now that bounded non-optimized recovery is attributable but still gated in sweep mode.
Why now: `/tmp/life2.plan12.out` now shows the `--max-functions 8` run prints `/* non-optimized fallback unavailable: heavy fallback lane disabled for sweep mode ... */` before `/* -- c (string intrinsic fallback) -- */`, while the direct `--addr 0x119d3` repro proves the ladder itself is already wired and attributable. The remaining blocker in the sweep lane is policy, not missing diagnostics.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py`; one new small helper under `/home/xor/vextest/inertia_decompiler/` if the policy logic would grow; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`.
Required edits: decide whether bounded non-optimized rescue should stay closed in sweep mode for `LIFE2.EXE` quality runs or whether one narrower policy should allow it; keep the decision driven by structured lane policy, not sample-specific names or rendered text; if the lane stays closed, keep the string-fallback-attribution wording stable and explicit.
Required tests: extend the existing sweep-disabled string-fallback CLI coverage in `test_x86_16_cli.py`; add one regression that locks the chosen sweep-lane policy for `LIFE2.EXE`-style `--max-functions` runs.
Verification commands: `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_cli.py -k "string_intrinsic or partial_timeout or failure_detail"`; `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 5 --max-functions 8 > /tmp/life2.plan1.out 2>&1`; `rg -n "sub_119d3|non-optimized fallback unavailable|string intrinsic fallback|heavy fallback lane disabled" /tmp/life2.plan1.out`.
Definition of done: the `LIFE2.EXE --max-functions 8` run reflects one explicit, tested policy choice for `sub_119d3`, with no ambiguity about why non-optimized recovery was or was not used.
Stop conditions: stop if the only way to open the sweep lane is sample-specific logic; stop if the real blocker is not policy but lower slice-recovery cost, and move to item 2.

2. Goal: Reduce bounded slice-recovery cost for `LIFE2.EXE` `sub_119d3` so at least one non-optimized attempt can complete before the string fallback on the direct `--addr` path.
Why now: `/tmp/life2.sub119d3.addr.out` shows the new per-attempt bound works, but all three attempts still end as `lean bounded attempt: TimeoutError`, `full-no-refs bounded attempt: TimeoutError`, and `full-with-refs bounded attempt: TimeoutError` before string fallback. The old blind spot is gone; the remaining issue is slice-recovery cost inside `_pick_function_lean()` / `_pick_function()`.
Edit targets: `/home/xor/vextest/inertia_decompiler/slice_recovery.py`; one new small helper under `/home/xor/vextest/inertia_decompiler/` if needed; `/home/xor/vextest/inertia_decompiler/cli.py` only for narrow wiring; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`.
Required edits: profile or trim the bounded slice attempts without adding sample-specific logic; prefer changing bounded attempt composition, preconditions, or cost guards over another timeout bump; keep the first structured failure detail and partial-output preference intact.
Required tests: keep the new bounded-attempt regressions in `test_x86_16_cli.py`; add one focused test for the chosen cost-control rule if a new helper is introduced.
Verification commands: `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_cli.py -k "non_optimized_slice or partial_timeout or string_intrinsic"`; `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x119d3 --timeout 5 > /tmp/life2.plan2.out 2>&1`; `rg -n "non-optimized fallback unavailable|string intrinsic fallback|shared-project slice|fresh-project slice|sub_119d3" /tmp/life2.plan2.out`.
Definition of done: the direct `--addr 0x119d3` repro either emits bounded non-optimized C/partial C or it fails with a sharper lower-layer reason than three generic bounded-attempt timeouts.
Stop conditions: stop if the hot path is below slice recovery and belongs in lift/CFG work; stop if the only apparent win is another timeout increase without a real cost change.
