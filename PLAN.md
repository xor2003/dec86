10. [x] Harness max-worker-iters handoff: route blocked worker exhaustion to reviewer instead of crashing the harness.
Goal: Keep the harness alive when a blocked task packet keeps returning non-zero remaining steps through the worker iteration budget.
Why now: `/home/xor/vextest/.codex_automation/cycles/20260407_220833_cycle001/worker.iter40.log:1-34` ends normally with `Global Remaining steps: 3`, `/home/xor/vextest/.codex_automation/cycles/20260407_220833_cycle001/status.worker-iter40.txt:1-4` marks the role `status=done`, but `/home/xor/vextest/.codex_automation/cycles/20260407_220833_cycle001/cycle.state.json:1-111` still leaves `steps.worker.status=\"running\"`, so `meta_harness/orchestrator.py` fell off the `MAX_WORKER_ITERS` edge and triggered crash-review instead of reviewer/planner escalation.
Edit targets: `/home/xor/vextest/meta_harness/orchestrator.py:2008-2170`; `/home/xor/vextest/meta_harness/tests/test_orchestrator.py:1194-1245`.
Required edits: 1. Replace the terminal `MAX_WORKER_ITERS` hard failure in `/home/xor/vextest/meta_harness/orchestrator.py:2170` with a normal `worker.stalled` state transition, status-file update, and reviewer handoff. 2. Keep existing repeated-no-progress and consecutive-failure escalation unchanged. 3. Add a regression test that drives two non-zero worker iterations and asserts the worker step ends as `stalled` with `max_worker_iters_exhausted` instead of raising.
Required tests: `uv run pytest /home/xor/vextest/meta_harness/tests/test_orchestrator.py -k 'worker_cycle_stalls_after_max_worker_iters or worker_cycle_stalls_after_repeated_no_progress_logs'`
Verification commands: `uv run pytest /home/xor/vextest/meta_harness/tests/test_orchestrator.py -k 'worker_cycle_stalls_after_max_worker_iters or worker_cycle_stalls_after_repeated_no_progress_logs'`
Definition of done: The harness advances from a maxed-out blocked worker loop into reviewer/planner handling without entering crash-review solely because `MAX_WORKER_ITERS` was exhausted.
Stop conditions: Stop if preserving reviewer handoff requires suppressing genuine harness exceptions unrelated to worker no-progress exhaustion.

11. [ ] Upstream boundary: keep the `0x1157c` far-pointer stack object fix explicitly blocked above local rewrite unless upstream angr exposes a stable pre-codegen hook.
Goal: Keep the `0x1157c` failure isolated to the pre-`StructuredCodeGenerator(...)` boundary and prevent repo-local late rewrite or CLI glue from absorbing it.
Why now: `/home/xor/vextest/.codex_automation/evidence.log:104-110` still emits `int sub_1157c(void) { sub_15d8(); /* do not return */ sub_15d8(); /* do not return */ }`, and `/home/xor/vextest/scripts/repro_decompiler_boundary.py:111-169` still reports `same_clinic_object=True`, `same_variable_manager_object=True`, `stack_object_0x1157c_widened=False`, `codegen_sub_15d8_call_count=2`, and `boundary_fixpoint=upstream_angr_cached_clinic_reuse_before_codegen` with `upstream_hook_note=no caller-visible callback exists between Clinic(...) and StructuredCodeGenerator(...)`.
Edit targets: `/home/xor/vextest/scripts/repro_decompiler_boundary.py:11-169`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:2246-2312`; `/home/xor/vextest/.venv/lib/python3.14/site-packages/angr/analyses/decompiler/decompiler.py:293-443`.
Required edits: 1. Keep `/home/xor/vextest/scripts/repro_decompiler_boundary.py:111-169` reporting the cached `Clinic`, variable-manager ids, per-offset stack objects, `codegen_sub_15d8_call_count`, and `boundary_fixpoint` for `Decompiler(generate_code=False)` followed by `Decompiler(regen_clinic=False)`. 2. Leave `/home/xor/vextest/inertia_decompiler/cli.py` unchanged unless the repro proves a caller-visible mutation point between cached clinic reuse and `StructuredCodeGenerator(...)`. 3. If `/home/xor/vextest/.venv/lib/python3.14/site-packages/angr/analyses/decompiler/decompiler.py:293-443` gains a stable hook, implement the smallest general pre-codegen mutation there so overlapping `[-2,+1]` far-pointer locals widen before codegen. 4. If the hook is still absent, keep this item blocked and update only the repro/test expectations.
Required tests: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'repro_decompiler_boundary_reports_blocked_narrow_stack_object or far_pointer_stack_local_width or far_pointer_store'`
Verification commands: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'repro_decompiler_boundary_reports_blocked_narrow_stack_object or far_pointer_stack_local_width or far_pointer_store'`; `uv run python /home/xor/vextest/scripts/repro_decompiler_boundary.py /home/xor/vextest/LIFE2.EXE --addr 0x1157c`; `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x1157c --timeout 6`
Definition of done: The duplicated `sub_15d8(); /* do not return */` body at `0x1157c` is either removed by a general pre-codegen fix upstream of `StructuredCodeGenerator(...)`, or this item remains explicitly blocked with current repro evidence showing no stable caller-visible hook.
Stop conditions: Stop local implementation as soon as the only remaining fix point is cached-clinic reuse inside upstream angr with no caller-visible hook.

12. [x] Whole-binary deadline promotion: stop emitting timeout/non-optimized fallback text for functions whose worker result is already complete by the deadline handoff.
Goal: Make whole-binary LIFE2 runs keep optimized or partial worker results when the decompiler already finished instead of downgrading them to `/* problem: timeout */`.
Why now: `/home/xor/vextest/.codex_automation/evidence.log:24-61` shows `Decompiler returned` plus `decompilation time ... 2.60s`, `1.85s`, and `1.10s` for `0x10010`, `0x100ea`, and `0x115bf`, but the emitted output is still `/* problem: timeout */` with non-optimized fallback text; the deadline/result handoff still lives in `/home/xor/vextest/inertia_decompiler/cli.py:16042-16090`, and the only existing coverage is the already-done-future tests at `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:3968-4105`.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py:16042-16090`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:3968-4105`.
Required edits: 1. In `/home/xor/vextest/inertia_decompiler/cli.py:16061-16074`, check `future.done()` before installing a synthetic timeout result and consume the worker result immediately when it is already available. 2. In `/home/xor/vextest/inertia_decompiler/cli.py:16078-16090`, add one bounded late-result collection pass for expired futures after shutdown so completed `ok` and `partial_payload` results replace synthetic timeout placeholders before `_emit_function_result(...)` runs. 3. Keep synthetic timeout results only for futures that still have no completed worker result after the bounded late-result pass. 4. Extend `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:3968-4105` with one case for `future.done()` at deadline and one case for a future that becomes done during the post-shutdown late-result pass.
Required tests: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'main_parallel_uses_late_success_after_deadline or main_parallel_promotes_late_partial_after_deadline or main_parallel_promotes_done_future_at_deadline or main_parallel_promotes_future_completed_during_late_collection'`
Verification commands: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'main_parallel_uses_late_success_after_deadline or main_parallel_promotes_late_partial_after_deadline or main_parallel_promotes_done_future_at_deadline or main_parallel_promotes_future_completed_during_late_collection'`; `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6`
Definition of done: A whole-binary LIFE2 run no longer prints timeout/non-optimized fallback output for any function whose worker already produced an `ok` result or `partial_payload` before the final result emission path.
Stop conditions: Stop if preserving the finished result requires sample-specific address/name rules instead of general future-state handling in the deadline path.

13. [ ] Tail-validation missing-coverage localization: expose which functions/stages were uncollected so the remaining coverage gap is actionable.
Goal: Make the tail-validation surface and console summary name the missing functions/stages behind partial or uncollected coverage instead of only reporting totals.
Why now: `/home/xor/vextest/angr_platforms/.cache/tail_validation_details/LIFE2.8f.06cfcead08bb.tail_validation_surface.json` reports `coverage_count=4` and `missing_stage_total=12` with empty `stage_hotspots` and empty `top_changed_functions`, so the current logs do not localize the missing-coverage family well enough to choose the next validator-facing fix; the relevant aggregation/rendering code is `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py:650-753,885-958`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/milestone_report.py:55-109`, and `/home/xor/vextest/inertia_decompiler/cli.py:4751-4799`.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py:650-753`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py:885-958`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/milestone_report.py:55-109`; `/home/xor/vextest/inertia_decompiler/cli.py:4751-4799`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_tail_validation.py:830-847`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_milestone_report.py:976-995`.
Required edits: 1. Extend `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py:681-753` so the surface carries a deterministic `top_missing_functions` summary grouped by function and missing stages. 2. Keep `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py:923-958` severity logic unchanged while adding the new missing-function rows only for `partial` and `uncollected` summaries. 3. Update `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/milestone_report.py:77-109` to print up to three missing-function lines when `missing_stage_total > 0` and there are no changed-function hotspots to explain the gap. 4. Update `/home/xor/vextest/inertia_decompiler/cli.py:4786-4799` only as needed to preserve the new surface fields in the JSON metadata payload. 5. Extend the focused tail-validation and milestone-report tests to assert the new `top_missing_functions` payload and console lines.
Required tests: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_tail_validation.py -k 'uncollected_separately_from_unknown or aggregate_reuses_record_fingerprint_cache or missing_function_summary'`; `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_milestone_report.py -k 'tail_validation_console_summary_calls_out_uncollected_coverage or tail_validation_console_summary_calls_out_missing_functions'`
Verification commands: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_tail_validation.py -k 'uncollected_separately_from_unknown or aggregate_reuses_record_fingerprint_cache or missing_function_summary'`; `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_milestone_report.py -k 'tail_validation_console_summary_calls_out_uncollected_coverage or tail_validation_console_summary_calls_out_missing_functions'`; `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6`
Definition of done: The tail-validation console/detail artifact for a partial or uncollected LIFE2 run identifies the missing functions/stages directly instead of only emitting coverage totals.
Stop conditions: Stop if the only available signal is still aggregate totals and the missing function/stage identity cannot be derived from the existing record set without inventing sample-specific metadata.

14. [ ] Seeded/cached LIFE2 discovery count: raise the current 8-function ceiling with general pre-entry follow-on recovery instead of display-only ranking tweaks.
Goal: Recover more of the shared LIFE/LIFE2 function bodies so LIFE2 whole-binary runs move beyond the current 8 recovered functions and closer to the 108-function LIFE oracle without importing LIFE helper metadata.
Why now: `/home/xor/vextest/.codex_automation/evidence.log:1-18` shows the active LIFE2 sweep still stops at `functions recovered: 8` from `restored 6 previously discovered function(s) from cache` plus `supplemental prologue scan recovered 2 additional function(s) near entry`, while `/home/xor/vextest/LIFE.dec:1-20` still reports `functions recovered: 108` for the same program with sidecar labels. The active discovery bottlenecks are `/home/xor/vextest/inertia_decompiler/cli.py:1806-1875`, `/home/xor/vextest/inertia_decompiler/cli.py:2383-2440`, and `/home/xor/vextest/inertia_decompiler/cli.py:2556-2595`.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py:1806-1875`; `/home/xor/vextest/inertia_decompiler/cli.py:2383-2440`; `/home/xor/vextest/inertia_decompiler/cli.py:2556-2595`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:3478-3529`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:3580-3623`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:3688-3705`.
Required edits: 1. In `/home/xor/vextest/inertia_decompiler/cli.py:2556-2595`, feed `_recover_seeded_exe_functions(...)` with prioritized pre-entry follow-on candidates when the recovered set still lacks a meaningful pre-entry body, reusing the existing covered-range and recovery-score heuristics instead of address/name hacks. 2. In `/home/xor/vextest/inertia_decompiler/cli.py:2383-2440`, keep `_recover_fast_exe_catalog(...)` overscanning beyond the display limit until it has tried the additional pre-entry body candidates, then rank and trim only after the richer candidate set exists. 3. In `/home/xor/vextest/inertia_decompiler/cli.py:1806-1875`, keep cached recovery supplementation aligned with the seeded path so cache restore and fresh seed recovery use the same body-follow-on heuristic and do not diverge. 4. Replace the stale display-only expectations in the targeted CLI tests with assertions about uncapped recovered-count growth and overscan behavior for pre-entry body candidates.
Required tests: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'main_reports_uncapped_seeded_function_count or main_reports_uncapped_cached_function_count or recover_fast_exe_catalog_overscans_seed_limit_before_trimming or recover_seeded_exe_functions_recovers_pre_entry_follow_on_candidates'`
Verification commands: `uv run pytest /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'main_reports_uncapped_seeded_function_count or main_reports_uncapped_cached_function_count or recover_fast_exe_catalog_overscans_seed_limit_before_trimming or recover_seeded_exe_functions_recovers_pre_entry_follow_on_candidates'`; `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6`
Definition of done: A whole-binary LIFE2 run recovers more than 8 functions through general seeded/cached recovery logic, and the added functions come from pre-entry/follow-on body discovery rather than sample-specific display ordering.
Stop conditions: Stop if increasing the LIFE2 count requires sample-specific seed ordering, hard-coded addresses, or helper metadata imported from LIFE.EXE.
