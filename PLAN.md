1. Goal: Reuse the bounded slice-recovery ladder inside the non-optimized rescue lane so `LIFE2.EXE` timeout cases can emit bounded C before string fallback.
Why now: `/home/xor/vextest/.codex_automation/evidence.log:163-205` shows `sub_119d3` still times out at 5s and drops to `/* -- c (string intrinsic fallback) -- */`; the same log now proves the old blind spot is gone because the CLI prints `heavy fallback lane disabled for sweep mode`, so the next blocker is rescue strength, not missing diagnostics.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py:937-1092`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:934-1105`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:5949-5995`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_string_corpus_anchors.py:14-41`.
Required edits: extract one shared bounded slice ladder helper from `_try_decompile_sidecar_slice()` and `_try_decompile_non_optimized_slice()`; make `_try_decompile_non_optimized_slice()` run lean, full-no-refs, and full-with-refs attempts with the same bounded policy; keep helper metadata off for peer EXE reuse; preserve partial codegen text and first structured failure detail on each miss; keep fallback order unchanged so partial C and non-opt C still beat string-intrinsic output.
Required tests: extend `test_try_decompile_non_optimized_slice_retries_full_recovery_after_lean_miss`; extend `test_main_falls_back_to_partial_timeout_before_asm_when_available` or add a sibling for non-opt-before-string; keep `test_monoprin_fimemset_emits_string_intrinsic_fallback_anchor`.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cli.py -k "non_optimized_slice or partial_timeout"`; `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_string_corpus_anchors.py`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 5 --max-functions 8 > /tmp/life2.plan1.out 2>&1`; `rg -n "sub_119d3|non-optimized fallback|string intrinsic fallback|partial timeout" /tmp/life2.plan1.out`.
Definition of done: on this local diff, the `sub_119d3` direct run emits bounded C or partial C before string fallback, and focused tests prove string anchors still stay on string fallback when rescue truly fails.
Stop conditions: stop if the bounded ladder still fails below slice recovery and the evidence points to lift/CFG, not CLI fallback policy; stop if the only winning path needs `LIFE.EXE` helper metadata or sample-specific logic; after one blocked try on the same local diff, route back to planner/reviewer.

2. Goal: Lock the exact non-optimized miss reason surface on the final string-fallback branch so future timeout cases stay attributable in direct and streaming output.
Why now: `/home/xor/vextest/inertia_decompiler/cli.py:13340-13400` now prints skip reasons when the heavy lane is closed, but the string branch still depends on mixed ad hoc sources (`allow_heavy_fallbacks`, `skip_heavy_fallbacks_for_result`, and optional non-opt outcome detail); this needs one small hardening pass before more LIFE2 timeout work lands.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py:13172-13421`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:1000-1036`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:4428-4498`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py:5936-5995`.
Required edits: factor one helper that computes the final non-opt-unavailable reason from lane policy and `NonOptimizedSliceOutcome`; call it from `_emit_function_result()` in both heavy-lane-closed and heavy-lane-open string-fallback branches; keep the reason sourced from structured status/detail only, never from rendered C or asm text; make the helper return deterministic wording for sweep-disabled, stage-closed, and bounded-attempt-failed cases.
Required tests: keep `test_try_decompile_non_optimized_slice_reports_failure_detail`; add or extend one `_emit_function_result()` CLI test for heavy-lane-closed string fallback; add or extend one CLI test for heavy-lane-open string fallback that consumes `_non_optimized_slice_failure_detail`.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cli.py -k "failure_detail or string_intrinsic or partial_timeout"`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 5 --max-functions 8 > /tmp/life2.plan2.out 2>&1`; `rg -n "non-optimized fallback unavailable|string intrinsic fallback|heavy fallback lane disabled" /tmp/life2.plan2.out`.
Definition of done: every string-fallback path prints one concrete non-opt miss reason derived from structured status data, and CLI tests lock both sweep-disabled and bounded-attempt-failed wording.
Latest check: 2026-04-14 added shared bounded slice ladder in `inertia_decompiler/slice_recovery.py`, fixed the direct `--addr` timeout path to render `outcome.rendered` instead of `NonOptimizedSliceOutcome(...)`, and raised the bounded non-opt runner budget to `20s` per project attempt; `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_cli.py -k "non_optimized_slice or partial_timeout"` passed `14` tests in `2.61s`, and `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_string_corpus_anchors.py -k monoprin_fimemset` passed in `58.22s`; but direct `LIFE2.EXE` repro `/home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x119d3 --timeout 5` still prints `shared-project slice: retry crashed: TimeoutError; fresh-project slice: retry crashed: TimeoutError` before `/* == c (string intrinsic fallback) == */`, with timestamps showing the shared-project bounded lane still runs about `26s` and the fresh-project retry about `20s`. Profiling that exact repro with `/usr/bin/time -v /home/xor/vextest/.venv/bin/python -m cProfile -o /tmp/life2.sub119d3.prof /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x119d3 --timeout 5` shows `43735065` calls in `55.18s`, `RSS ~= 377832 kB`, and the dominant hot path is `_pick_function_lean()` feeding `pyvex.lifting.util.vex_helper.get_bit/op_extract_lsb` (~`46s` cumulative), so the next cycle should target bounded slice recovery/lifting cost itself, not another timeout bump.
Stop conditions: stop if the required detail is missing upstream from `NonOptimizedSliceOutcome`; stop if the only available explanation would come from rendered output text instead of typed status/detail fields.
