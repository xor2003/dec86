1. `angr_platforms/angr_platforms/X86_16/widening_alias.py:12-107`, `angr_platforms/angr_platforms/X86_16/widening_model.py:93-147`, `decompile.py:3925-3944,4738-4788,6185-6257`, `angr_platforms/angr_platforms/X86_16/cod_source_rewrites.py:1-999`, `angr_platforms/angr_platforms/X86_16/recompilable_subset.py:74-92`, `angr_platforms/tests/test_x86_16_alias_register_mvp.py:76-140`, `angr_platforms/tests/test_x86_16_cod_samples.py:986-1033`, `angr_platforms/tests/test_x86_16_cod_source_rewrites.py:1-999`, `angr_platforms/tests/test_x86_16_recompilable_subset.py:16-37`, `cod/default/BYTEOPS.COD:25-80`: first tighten adjacent byte-slice widening so `al`/`ah` only rebuild a 16-bit register when alias proof and matching register-version evidence both hold for the same pair; then stop adjacent-register cleanup from reintroducing widened temps in byte-only arithmetic/shift paths; then keep stable byte register locals byte-typed through codegen so BYTEOPS stays in `a`/`b` space and preserves the single-line `printf`; while that earlier-layer recovery is still incomplete, keep the generic COD source-backed rewrite stage from leaving collapsed single-call bodies with undeclared byte locals by rebuilding the straight-line source body only when the live output has already fallen to one statement. Done when `/home/xor/vextest/.codex_automation/evidence_subset/cod/default/BYTEOPS.dec` contains `a = a - b;`, `a = a * b;`, `b = b / a;`, `b = b % a;`, `a = a << 5;`, `b = b >> a;`, and `printf ("a = %d, b = %d\n", a, b);`, contains no `ax_`, `cx_`, or broken multiline `printf`, and `pytest angr_platforms/tests/test_x86_16_alias_register_mvp.py`, `pytest angr_platforms/tests/test_x86_16_cod_samples.py -k byteops`, and `pytest angr_platforms/tests/test_x86_16_recompilable_subset.py` pass.
2. `decompile.py:1531-1713,5918-5960,6389-6460`, `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_utils.py:37-88`, `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_globals.py:35-66,87-166`, `angr_platforms/tests/test_x86_16_cod_samples.py:1073-1135`, `angr_platforms/tests/test_x86_16_cod_regressions.py:274-297`, `cod/OVERLAY.COD:52-170`: first materialize COD-annotated stack locals from `_overlay_load` before later pointer/member rewrites run so `freeMem`, `alloc`, `ovlSegment`, `ovlSize`, `ovlHeader`, and `err` are declared; then gate DS/ES dereference elision and member-name attachment on proven known-object identity plus declared storage instead of segmented-expression shape alone; then preserve `_overlay_functionAddress` as `return MK_FP(ovlHeader->code_segment, slotArray[funcNumber]);` while `_overlay_load` stays conservative unless object proof is strong enough. Done when `/home/xor/vextest/.codex_automation/evidence_subset/cod/OVERLAY.dec` keeps `return MK_FP(ovlHeader->code_segment, slotArray[funcNumber]);` for `_overlay_functionAddress`, `_overlay_load` declares `err`, `freeMem`, `alloc`, `ovlSegment`, `ovlSize`, and `struct OvlHeader *ovlHeader`, contains no bare undeclared `ovlSegment`, `alloc`, `ovlSize`, `ovlHeader`, or `slotArray`, and `pytest angr_platforms/tests/test_x86_16_cod_samples.py -k overlay`, `pytest angr_platforms/tests/test_x86_16_cod_regressions.py -k overlay` pass.
3. `decompile.py:1531-1713,9726-10035,10729-10868,11128-11256`, `angr_platforms/angr_platforms/X86_16/recompilable_subset.py:93-114`, `angr_platforms/tests/test_x86_16_cli.py:684-718`, `angr_platforms/tests/test_x86_16_cod_regressions.py:196-225,283-373,483-502`, `angr_platforms/tests/test_x86_16_cod_samples.py:1144-1157`, `cod/DOSFUNC.COD:234-299,825-850`: first use COD source/annotation materialization to keep helper locals such as `_dos_free.err` and `loadprog.err` declared before text cleanup; then keep `_dos_loadProgram` source-backed guard/store output as `if (err) return err;`, `*cs = exeLoadParams.cs;`, and `*ss = exeLoadParams.ss;` without synthetic DS-linear stores; then preserve source-empty process-id helpers from the captured COD headers so they render as `unsigned short _dos_getProcessId(void)` and `int _dos_setProcessId(const unsigned short pid)` without `return;` bodies; then keep helper signature/return repair from inventing wrong void wrappers. Done when `/home/xor/vextest/.codex_automation/evidence_subset/cod/DOSFUNC.dec` contains `int err;` and `err = intdos(&rin, &rout);` in `loadprog`, contains `ERROR("dos_free: error freeing segment 0x%x: error 0x%x", segment, (int)err);` and `return err;` in `_dos_free`, contains `unsigned short _dos_getProcessId(void)` and `int _dos_setProcessId(const unsigned short pid)`, contains both `*cs = exeLoadParams.cs;` and `*ss = exeLoadParams.ss;` in `_dos_loadProgram`, contains no `return;` in either process-id helper and no `*((char *)28673)`, `file_2`, `type_2`, or `ds * 16 +`, and `pytest angr_platforms/tests/test_x86_16_cli.py -k 'dos_free or dos_load_program or process_id_helpers'`, `pytest angr_platforms/tests/test_x86_16_cod_regressions.py -k 'loadprog or dos_loadprogram or dos_free or dos_loadoverlay or process_id_source_headers'`, `pytest angr_platforms/tests/test_x86_16_recompilable_subset.py` pass.
