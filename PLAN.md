1. [done] Reproduce the current LIFE2 hard-timeout behavior on `0x12320` before more optimization work. Target files: `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/runtime_support.py`, `/home/xor/vextest/PLAN.md`. Concrete result: `/home/xor/vextest/LIFE2.12320.err` shows `0x12320` no longer takes the old 494.00s; `--addr 0x12320 --timeout 6` exits in 13.08s with bounded timeout/fallback output and peak RSS 181332 KB, while `/home/xor/vextest/LIFE2.current.err` shows fork/COW hard timeouts at 8s for other expensive functions with peak RSS 208428 KB. Definition of done: the new artifact or console output shows `0x12320` either decompiled under the requested limit or reported as timed out with an explicit requested/hard timeout, with no `can't start new thread`, no `CS_ERR_MEM`, and no multi-minute timestamp gap.
2. [done] Improve ambiguous LIFE2 timeout/cache/fallback messages and enforce success-only caching. Target files: `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/PLAN.md`. Concrete result: `function_decompile` cache writes only `status == "ok"` results; timeout/error/fallback results are not stored; old cached timeout/error records are ignored with an explicit debug message; successful functions completed before a later timeout remain cacheable; `/home/xor/vextest/LIFE2.success_only_cache2.err` shows successful function cache hits but no `timeout cache hit` lines. Definition of done: a LIFE2 timeout path no longer reads as if a cached failure is a valid result, and tests cover both ignoring old failed cache records and not writing new timeout records.
3. [done] Add an end-of-run LIFE2 decompilation timing summary. Target files: `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/work_items.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/PLAN.md`. Concrete result: `FunctionWorkResult.elapsed` is carried through fork IPC, non-cached timeout attempts, and summary emission; cached successful results are marked with `from_cache=True` and excluded from current-run timing rows; `/home/xor/vextest/LIFE2.success_only_cache2.err` finishes in 19.88s because 9 timed-out functions are intentionally retried instead of cached, and stale successful-cache timing rows are excluded. Definition of done: the end of a LIFE2 run can explain current bottlenecks without mixing in old timings from cached results.
4. [done] Classify the first LIFE2 timeout/fallback assembly patterns without guessing high-level C. Target files: `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/PLAN.md`. Concrete result: fallback asm with `rep movs`, `rep stos`, `repne scas`, or `repe cmps` now gets a conservative note such as `assembly pattern: x86 string-instruction copy loop; evidence from asm, not guessed C`; `/home/xor/vextest/LIFE2.service4.err` shows this for addresses including `0x119d3`, `0x1223b`, `0x1150c`, `0x11668`, `0x116ba`, and `0x1175e`. Definition of done: fallback output gives useful evidence-based context while still showing asm and not pretending guessed C was recovered.
5. [done] Separate sidecar metadata cache invalidation from CLI/decompiler output changes. Target files: `/home/xor/vextest/inertia_decompiler/cache.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/PLAN.md`. Concrete result: FLAIR/PAT sidecar metadata now uses `SIDECAR_METADATA_CACHE_SOURCE_FILES`, which includes `sidecar_metadata.py`, `sidecar_parsers.py`, `omf_pat.py`, and signature/parser files but excludes `cli.py`; `/home/xor/vextest/LIFE2.sidecar_cache_hot.cprof` no longer shows `_match_flair_startup_pat_functions`, `match_pat_modules`, or hyperscan database compilation in the top cumulative-time entries. Definition of done: changing CLI output or function-decompile policy no longer forces repeated FLAIR/PAT sidecar metadata matching, while parser/signature changes still invalidate the metadata cache.
6. [pending] Improve x86-16 C quality at the earliest correct layer for recurring LIFE2 artifacts. Target files: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/`, `/home/xor/vextest/angr_platforms/tests/`, `/home/xor/vextest/PLAN.md`. Concrete work: use focused examples from LIFE2 output (`sub_140d(); /* do not return */`, `flags = ...`, `goto vvar_*`, segmented memory/stack-slot confusion) to choose alias/widening/helper-trait fixes before late rewrite cleanup. Definition of done: at least one recurring LIFE2 readability issue improves through a general tested alias/widening/trait mechanism, not an address or symbol-name hack.
