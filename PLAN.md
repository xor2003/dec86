1. [completed] `/home/xor/vextest/inertia_decompiler/sidecar_parsers.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: run startup PAT function-signature search across the whole mapped image instead of only probing entry bytes. Functions/tests/scripts: `_detect_flair_metadata`, `_match_flair_startup_pat_functions`, `_load_flair_startup_pat_modules`, `test_detect_flair_metadata_searches_startup_pats_across_whole_binary`, and `uv run pytest -q /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'detect_flair_metadata or load_lst_metadata_forwards_flair_parameters_without_global_args'`. Done when startup PAT modules are matched against `main_object.min_addr..max_addr`, recovered labels/ranges can land away from `project.entry`, and the metadata tests assert the whole-image backend call explicitly.
2. [completed] `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: codify the real `LIFE.EXE` vs `LIFE2.EXE` evidence delta as a deterministic regression. Functions/tests/scripts: `_load_lst_metadata`, `_visible_code_labels`, `test_life2_without_peer_exe_metadata_has_no_sidecars_but_life_does`, and direct metadata probes against `LIFE.EXE` and `LIFE2.EXE`. Done when the regression captures the true baseline: `LIFE.EXE` has a large visible sidecar-backed catalog, while `LIFE2.EXE` natively has only hidden startup FLAIR metadata and no visible helper-backed function catalog.
3. [completed-oracle-only] `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add a deterministic peer-binary evidence lane that can compare `LIFE2.EXE` against a nearby sidecar-backed sibling executable only when code bytes match exactly at the same entry and across the full claimed span. Functions/tests/scripts: `_peer_exe_family_candidates`, `_exact_function_span_matches`, `_merge_peer_function_catalog`, `_discover_peer_exe_catalog_matches`, `test_peer_exe_catalog_requires_exact_span_byte_match`, `test_discover_peer_exe_catalog_matches_merges_exact_sibling_catalog`, and `test_life2_peer_catalog_oracle_requires_explicit_helper_call`. Done when the helper remains available for oracle use, but `LIFE2.EXE` no longer silently imports peer labels/ranges during normal decompilation.
4. [superseded] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/README.md`: consume peer-derived catalog at the live discovery/display layer. Superseded because `LIFE.EXE` is now treated as an oracle for improving `LIFE2.EXE`, not as a runtime dependency of `LIFE2.EXE`.
5. [superseded] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add a live peer-native sidecar fallback hook. Superseded because peer-backed decompilation is no longer allowed in the default `LIFE2.EXE` decompilation lane.
6. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: make exact-region sidecar recovery detect and retry obviously truncated lean CFG results before giving up. Functions/tests/scripts: `_exact_region_recovery_looks_truncated`, `_recover_lst_function`, `test_recover_lst_function_retries_full_exact_region_when_lean_result_is_truncated`, `test_try_decompile_sidecar_slice_retries_with_broader_exact_region_recovery`, and `uv run pytest -q /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'recover_lst_function_retries_full_exact_region_when_lean_result_is_truncated or try_decompile_sidecar_slice_retries_with_broader_exact_region_recovery'`. Done when exact sidecar spans such as `main`, `init_life`, and `init_buf` no longer stop at the first tiny lean result without first retrying broader CFG recovery inside the same bounded region.
7. [superseded] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: peer COD source fallback for `LIFE2.EXE`. Superseded because `LIFE2.EXE` should no longer depend on `LIFE.EXE` during normal decompilation.
8. [superseded] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: peer sidecar bundle caching for runtime recovery. Superseded with the same decoupling rule: keep peer comparison only as oracle/harness tooling, not the live decompilation path.
9. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: decouple default `LIFE2.EXE` decompilation from `LIFE.EXE` by removing live peer-catalog import and live peer sidecar fallback, while keeping exact-span peer comparison available as an explicit oracle helper. Functions/tests/scripts: `_load_lst_metadata`, `_try_decompile_peer_sidecar_slice`, `_discover_peer_exe_catalog_matches`, `test_life2_default_metadata_stays_independent_from_life_peer_catalog`, and `test_life2_peer_catalog_oracle_requires_explicit_helper_call`. Done when normal `LIFE2.EXE` decompilation no longer uses `peer_exe` metadata or `peer sidecar fallback`, but the explicit oracle helper can still compare the two binaries for development work.
10. [pending] `/home/xor/vextest/PLAN.md`, `/home/xor/vextest/meta_harness/README.md`, `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: continue improving `LIFE2.EXE` through the harness sweep lane using `LIFE.EXE` only as an offline comparison oracle. Functions/tests/scripts: a new sweep packet centered on `LIFE2.EXE`, `python -m meta_harness --fresh`, the sweep/checker/planner flow from `meta_harness/README.md`, direct no-peer probes such as `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x10010 --timeout 6`, and a focused regression that locks whatever new native `LIFE2.EXE` recovery wins the sweep produces. Done when the next harness cycle targets `LIFE2.EXE` alone in the live lane, records any `LIFE.EXE` comparison strictly as oracle evidence, and lands at least one new no-peer `LIFE2.EXE` decompilation improvement with regression coverage.
