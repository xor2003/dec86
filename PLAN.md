1. [completed] `/home/xor/vextest/inertia_decompiler/sidecar_parsers.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: run startup PAT function-signature search across the whole mapped image instead of only probing entry bytes. Functions/tests/scripts: `_detect_flair_metadata`, `_match_flair_startup_pat_functions`, `_load_flair_startup_pat_modules`, `test_detect_flair_metadata_searches_startup_pats_across_whole_binary`, and `uv run pytest -q /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'detect_flair_metadata or load_lst_metadata_forwards_flair_parameters_without_global_args'`. Done when startup PAT modules are matched against `main_object.min_addr..max_addr`, recovered labels/ranges can land away from `project.entry`, and the metadata tests assert the whole-image backend call explicitly.
2. [completed] `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: codify the real `LIFE.EXE` vs `LIFE2.EXE` evidence delta as a deterministic regression. Functions/tests/scripts: `_load_lst_metadata`, `_visible_code_labels`, `test_life2_without_peer_exe_metadata_has_no_sidecars_but_life_does`, and direct metadata probes against `LIFE.EXE` and `LIFE2.EXE`. Done when the regression captures the true baseline: `LIFE.EXE` has a large visible sidecar-backed catalog, while `LIFE2.EXE` natively has only hidden startup FLAIR metadata and no visible helper-backed function catalog.
3. [completed] `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add a deterministic peer-binary evidence lane that borrows function starts/ranges/names from a nearby sidecar-backed sibling executable only when code bytes match exactly at the same entry and across the full claimed span. Functions/tests/scripts: `_peer_exe_family_candidates`, `_exact_function_span_matches`, `_merge_peer_function_catalog`, `_discover_peer_exe_catalog_matches`, `test_peer_exe_catalog_requires_exact_span_byte_match`, `test_discover_peer_exe_catalog_matches_merges_exact_sibling_catalog`, and `test_life2_reuses_life_function_catalog_when_spans_are_identical`. Done when `LIFE2.EXE` imports exact byte-identical function labels/ranges from `LIFE.EXE` with an explicit `peer_exe` source marker, while any non-identical span is rejected.
4. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/README.md`: consume the peer-derived catalog at the discovery/display layer. Functions/tests/scripts: `_rank_exe_function_seeds`, `_rank_labeled_function_entries`, `test_life2_peer_catalog_ranks_application_functions_before_runtime_helpers`, and the smoke command `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6 --max-functions 8`. Done when `LIFE2.EXE` surfaces application routines first (`main`, `init_*`, `draw_*`, etc.) instead of burning the function cap on startup/runtime helpers such as `astart`, `chkstk`, `atol`, and `exit`.
5. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: add a peer-native sidecar decompilation fallback hook so exact-byte peer matches can attempt a bounded sidecar slice on the sibling executable that still has COD/CodeView metadata. Functions/tests/scripts: `_try_decompile_peer_sidecar_slice`, `test_try_decompile_peer_sidecar_slice_uses_native_peer_metadata`, and focused `peer_exe` test runs. Done when the helper exists, is regression-covered, and is only attempted for `peer_exe` functions with an exact span match.
6. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: make exact-region sidecar recovery detect and retry obviously truncated lean CFG results before giving up. Functions/tests/scripts: `_exact_region_recovery_looks_truncated`, `_recover_lst_function`, `test_recover_lst_function_retries_full_exact_region_when_lean_result_is_truncated`, `test_try_decompile_sidecar_slice_retries_with_broader_exact_region_recovery`, and `uv run pytest -q /home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py -k 'recover_lst_function_retries_full_exact_region_when_lean_result_is_truncated or try_decompile_sidecar_slice_retries_with_broader_exact_region_recovery'`. Done when exact sidecar spans such as `main`, `init_life`, and `init_buf` no longer stop at the first tiny lean result without first retrying broader CFG recovery inside the same bounded region.
7. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: improve actual `LIFE2.EXE` application-function output by allowing exact-byte peer matches to fall back to peer COD source text when bounded sidecar decompilation stays empty. Functions/tests/scripts: `_try_decompile_sidecar_slice`, `_render_cod_source_function_text`, `_sidecar_cod_metadata_for_function`, `_discover_peer_exe_catalog_matches`, `test_try_decompile_sidecar_slice_falls_back_to_cod_source_when_decompiler_stays_empty`, `test_discover_peer_exe_catalog_matches_merges_exact_sibling_catalog`, and the direct comparison commands `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x10010 --timeout 6`, `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x100ea --timeout 6`, and `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x10179 --timeout 6`. Done when `main`, `init_life`, and `init_buf` on `LIFE2.EXE` cross from raw asm fallback to `c (peer sidecar fallback)` using exact-span peer evidence plus real COD source lines from `LIFE.EXE`.
8. [completed] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/sidecar_metadata.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: cache peer sidecar bundles so `LIFE2.EXE` stops rebuilding `LIFE.EXE` metadata for every peer-backed function. Functions/tests/scripts: `_load_peer_sidecar_bundle`, `_try_decompile_peer_sidecar_slice`, `_discover_peer_exe_catalog_matches`, `test_try_decompile_peer_sidecar_slice_reuses_cached_peer_bundle`, `test_discover_peer_exe_catalog_matches_merges_exact_sibling_catalog`, and the comparison commands `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE.EXE --timeout 6 --max-functions 8` plus `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --timeout 6 --max-functions 8`. Done when repeated peer-backed functions in `LIFE2.EXE` reuse the same loaded sibling project/metadata, including the first bundle discovered during peer catalog import, instead of paying an extra peer build/load before fallback.
9. [pending] `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`: reduce reliance on peer COD source by improving bounded exact-region CFG recovery for exact sidecar spans that currently collapse to the same 3-block skeleton. Functions/tests/scripts: `_pick_function`, `_pick_function_lean`, `_recover_lst_function`, `_try_decompile_sidecar_slice`, `_try_decompile_peer_sidecar_slice`, `FunctionWorkResult` emission, a new bounded CFG-materialization regression such as `test_exact_region_cfg_rescue_materializes_later_branch_targets`, and the direct comparison commands `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE.EXE --addr 0x10010 --timeout 6` and `uv run python /home/xor/vextest/decompile.py /home/xor/vextest/LIFE2.EXE --addr 0x10010 --timeout 6`. Done when exact-region recovery can emit native decompiler C for more peer-backed application functions without needing the COD-source fallback path.
