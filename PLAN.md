1. Goal: Finish Batch A by replacing the stale COD changed baseline with one current full no-skip artifact on the unified path and record the exact artifact paths now in use.
Why now: `.codex_automation/evidence.log:1-157` only shows a focused LIFE2 timeout at `sub_119d3`; the latest full COD evidence already exists at `angr_platforms/.cache/decompile_cod_dir/cod-c7d08c1cd3a5.timeout20.tail_validation_console.json` and `angr_platforms/.cache/tail_validation_details/cod-c7d08c1cd3a5.timeout20.tail_validation_surface.json`, but `baseline=unavailable` there means Batch A is still open.
Edit targets: `scripts/decompile_cod_dir.py:990-1035`, `scripts/decompile_cod_dir.py:1298-1336`, `scripts/run_plan3_checks.py:89-182`, `angr_platforms/.cache/tail_validation_baselines/`, `.codex_automation/evidence.log`, `PLAN.md`.
Required edits: Run the unified full COD command with `--write-tail-validation-baseline` at `timeout20`; capture the written baseline path, console cache path, and detail artifact path; append those exact paths plus the resulting counts to `.codex_automation/evidence.log`; update this item with the new baseline identifier and remove any stale `changed=16` wording.
Required tests: No source edits unless the rerun proves a path/logging bug; if code changes are needed, run `angr_platforms/tests/test_decompile_cod_dir_parallelism.py`, `angr_platforms/tests/test_x86_16_tail_validation.py`, and `angr_platforms/tests/test_x86_16_milestone_report.py`.
Verification commands: `.venv/bin/python scripts/run_plan3_checks.py --include-cod-sweep --allow-cod-failures --cod-dir cod --cod-timeout 20 --cod-max-workers 1 --cod-max-memory-mb 4096 --cod-subprocess-timeout 180 --write-tail-validation-baseline`; `sed -n '1,80p' angr_platforms/.cache/decompile_cod_dir/cod-c7d08c1cd3a5.timeout20.tail_validation_console.json`; `sed -n '1,120p' angr_platforms/.cache/tail_validation_baselines/cod-c7d08c1cd3a5.timeout20.json`; `tail -n 40 .codex_automation/evidence.log`.
Definition of done: One current timeout20 full-COD console summary, one matching detail artifact, one written baseline JSON, and one evidence-log entry all agree on the same changed/uncollected identities with no dropped records.
Stop conditions: Stop if the rerun reproduces a new path-label mismatch, if the baseline filename does not match the console/detail label, or if the rerun changes worker accounting without a code diff since the prior attempt.

2. Goal: Make the active changed-family report actionable for the current dominant family instead of routing the whole cluster to generic structuring.
Why now: `cod-c7d08c1cd3a5.timeout20.tail_validation_surface.json` shows `changed_function_count=32`, with `family[31] control-flow/guard delta` across 28 functions and verdict text dominated by postprocess condition rewrites; `angr_platforms/tests/test_x86_16_tail_validation_routing.py:4-46` still routes that family to `structuring_sequences.py`.
Edit targets: `angr_platforms/angr_platforms/X86_16/tail_validation.py:373-419`, `angr_platforms/angr_platforms/X86_16/tail_validation.py:691-845`, `angr_platforms/angr_platforms/X86_16/milestone_report.py:54-147`, `angr_platforms/tests/test_x86_16_tail_validation_routing.py:4-46`, `angr_platforms/tests/test_x86_16_tail_validation.py:972-1224`.
Required edits: Split `control-flow/guard delta` routing by observed verdict signal so stack-projection/zero-flag normalization deltas point at postprocess files while true CFG-shape deltas keep pointing at structuring; surface the selected likely layer and next root-cause file in both the detail artifact and console summary helpers without changing verdict semantics.
Required tests: `angr_platforms/tests/test_x86_16_tail_validation_routing.py`; `angr_platforms/tests/test_x86_16_tail_validation.py -k "surface_groups_changed_observables_into_families or baseline_comparison_distinguishes_match_improve_and_regress or surface_annotation_includes_baseline_counts"`; `angr_platforms/tests/test_x86_16_milestone_report.py`.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_tail_validation_routing.py angr_platforms/tests/test_x86_16_tail_validation.py -k "surface_groups_changed_observables_into_families or baseline_comparison_distinguishes_match_improve_and_regress or surface_annotation_includes_baseline_counts" angr_platforms/tests/test_x86_16_milestone_report.py`; `sed -n '1,120p' angr_platforms/.cache/decompile_cod_dir/cod-c7d08c1cd3a5.timeout20.tail_validation_console.json`.
Definition of done: The current dominant family reports a postprocess root-cause file when its verdict signal is projection/flag cleanup noise, and the routing tests lock that mapping.
Stop conditions: Stop if the verdict strings are too weak to distinguish postprocess from structuring; add the missing signal first instead of guessing a layer.

3. Goal: Remove the current postprocess control-flow/guard delta family at the earliest correct layer by fixing projection and flag-condition rewrites, not by weakening validation.
Why now: The current timeout20 console summary shows repeated verdicts that replace `SS` stack-dereference forms with `stack:+0x2` and `flags` tests with direct register/zero-compare forms; the concrete candidate passes are `_simplify_boolean_cites_8616`, `_rewrite_flag_condition_pairs_8616`, and `_fix_interval_guard_conditions_8616`.
Edit targets: `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_simplify.py:52-220`, `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_flags.py:143-348`, `angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py:43-91`, `angr_platforms/tests/test_x86_16_tail_validation.py:510-620`, `angr_platforms/tests/test_x86_16_cli.py:9457-9808`.
Required edits: Reproduce one current changed verdict from the timeout20 artifact in a focused unit test; tighten the simplify/flag rewrite so it only rewrites when the resulting live-out condition fingerprint remains equivalent; keep per-pass validation enabled and, if needed, reorder the affected pass specs only when the proof stays at postprocess rather than rewrite-time guessing.
Required tests: `angr_platforms/tests/test_x86_16_tail_validation.py -k "normalizes_zero_flag_compare_projection_noise or normalizes_ss_stack_dereference_to_stack_write"`; `angr_platforms/tests/test_x86_16_cli.py -k "TIDShowRange or DrawRadarAlt or small_cod_logic_batch"`; `angr_platforms/tests/test_x86_16_package_exports.py -k decompiler_postprocess`.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_tail_validation.py -k "normalizes_zero_flag_compare_projection_noise or normalizes_ss_stack_dereference_to_stack_write"`; `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_cli.py -k "TIDShowRange or DrawRadarAlt or small_cod_logic_batch"`; `.venv/bin/python scripts/decompile_cod_dir.py cod --timeout 20 --max-workers 1 --max-memory-mb 4096 --subprocess-timeout 180 --cod-file COCKPIT.COD --proc-name _TIDShowRange --proc-name _DrawRadarAlt --allow-cod-failures`.
Definition of done: At least one representative changed family member drops from changed to passed without increasing unknown/uncollected counts, and the new unit tests pin the equivalence rule that made it possible.
Stop conditions: Stop if the only way to pass is to normalize text without preserving tail-validation equivalence; then back out and move the fix earlier than postprocess.

4. Goal: Make timeout fallback prefer partial optimized C in both direct and aggregate lanes so LIFE2 timeouts stop dropping straight to asm/details when partial code is available.
Why now: `.codex_automation/evidence.log:118-157` shows `LIFE2.EXE` timing out at `sub_119d3` and falling back to asm-only; the current CLI code already has partial-timeout plumbing in `inertia_decompiler/cli.py:3120-3152`, `12135-12274`, and `12944-13010`, but the focused evidence shows that path is not delivering optimized partial output for this case.
Edit targets: `inertia_decompiler/cli.py:3120-3152`, `inertia_decompiler/cli.py:12145-12274`, `inertia_decompiler/cli.py:12944-13010`, `inertia_decompiler/work_items.py:49-127`, `angr_platforms/tests/test_x86_16_cli.py:3890-3995`, `angr_platforms/tests/test_x86_16_cli.py:4200-4332`, `angr_platforms/tests/test_x86_16_cli.py:9457-9808`.
Required edits: Trace why the direct EXE timeout path loses `partial_payload` for helper-free binaries; preserve and emit partial optimized text before non-optimized or asm fallback whenever `_decompile_function_with_stats` captured codegen text; keep tail-validation snapshots tied to the fallback actually emitted; update display status so partial-timeout output is counted as decompiled output quality, not raw failure.
Required tests: `angr_platforms/tests/test_x86_16_cli.py -k "partial timeout or nonoptimized fallback or TIDShowRange or DrawRadarAlt or small_cod_logic_batch"`; `angr_platforms/tests/test_decompile_cod_dir_parallelism.py -k timeout`; `angr_platforms/tests/test_x86_16_tail_validation.py -k current_run_tail_validation`.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_cli.py -k "partial timeout or nonoptimized fallback or TIDShowRange or DrawRadarAlt or small_cod_logic_batch"`; `.venv/bin/python -m pytest -q angr_platforms/tests/test_decompile_cod_dir_parallelism.py -k timeout`; `.venv/bin/python decompile.py LIFE2.EXE --max-functions 8 --timeout 5`.
Definition of done: The focused LIFE2 repro no longer ends at asm-only when partial optimized text exists, and the aggregate CLI path prints `/* -- c (partial timeout) -- */` before any asm fallback for the same family.
Stop conditions: Stop if the fix depends on LIFE-specific helper metadata, peer catalog imports, or sample-address exceptions.

5. Goal: Measure LIFE2 against LIFE as the same codebase without helper metadata and lock the parity gap to concrete function-count and output-quality evidence.
Why now: Current evidence shows `LIFE2.EXE` queueing 96 likely non-library functions and timing out on `sub_119d3`, but there is no current side-by-side measurement against `LIFE.EXE` recorded in `.codex_automation/evidence.log`.
Edit targets: `.codex_automation/evidence.log`, `README.md` if a new reproducible command needs documenting, `PLAN.md`.
Required edits: Run matched helper-free direct decompilation commands for `LIFE.EXE` and `LIFE2.EXE`; record queued function counts, displayed decompiled count, timeout/fallback count, and one or two representative function diffs; write those concrete numbers into `.codex_automation/evidence.log` and update this item with the remaining parity gap.
Required tests: No source tests unless command drift requires wrapper changes; if wrappers change, run `angr_platforms/tests/test_x86_16_cli.py -k "life2 or signature metadata"`.
Verification commands: `.venv/bin/python decompile.py LIFE.EXE --max-functions 8 --timeout 5`; `.venv/bin/python decompile.py LIFE2.EXE --max-functions 8 --timeout 5`; `rg -n "LIFE\\.EXE|LIFE2\\.EXE|summary: decompiled|timed out|fell back to asm" .codex_automation/evidence.log -S`.
Definition of done: Evidence log contains one same-settings LIFE vs LIFE2 comparison with function-count, timeout/fallback, and representative output-quality differences stated concretely.
Stop conditions: Stop if the only way to improve LIFE2 is to import LIFE helper metadata; keep the comparison oracle-only and helper-free.

6. Goal: Keep traits->types->objects and dedicated storage/object IR moving at the earliest correct layer instead of leaking object guesses into late rewrites.
Why now: User priority still requires correctness first and comparable LIFE2 output quality; the earlier plan’s Phase 7 and Phase 12 remain unfinished, and any new object/layout improvement for LIFE2 must land in the dedicated evidence-bearing layers rather than postprocess cleanup.
Edit targets: `inertia_decompiler/cli_access_profiles.py`, `inertia_decompiler/cli_access_object_hints.py`, `inertia_decompiler/cli_access_trait_rewrite.py`, `inertia_decompiler/cli_storage_objects.py`, `inertia_decompiler/cli_far_pointer_stack.py`, `inertia_decompiler/cli_cod_globals.py`, `angr_platforms/angr_platforms/X86_16/alias_model.py`, `angr_platforms/angr_platforms/X86_16/segmented_memory_reasoning.py`, `angr_platforms/angr_platforms/X86_16/recompilable_subset.py`, `inertia_decompiler/cli.py`.
Required edits: Only take on one object/layout gap that is backed by current LIFE/LIFE2 evidence; move the decision into storage/object IR or trait evidence; add the smallest regression proving the decision is alias-first and segment-safe; do not patch around the issue in postprocess or text cleanup.
Required tests: `angr_platforms/tests/test_x86_16_storage_domain_alias.py`; `angr_platforms/tests/test_x86_16_segmented_memory.py`; `angr_platforms/tests/test_x86_16_recompilable_subset.py`; the smallest affected access-trait/object test.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_storage_domain_alias.py angr_platforms/tests/test_x86_16_segmented_memory.py angr_platforms/tests/test_x86_16_recompilable_subset.py`; run the focused LIFE/LIFE2 repro that motivated the change.
Definition of done: The chosen object/layout improvement is expressed in storage/object IR or trait evidence, is regression-backed, and does not require a late rewrite special case.
Stop conditions: Stop if the candidate fix cannot be explained in terms of alias state, segmented association, or typed object evidence.

7. Goal: Consume the existing CFG snapshot/block-ownership artifacts in one recovery or validation consumer before adding more structuring machinery.
Why now: Phase 13 is open only on adoption; the current plan must preserve it, but current evidence does not justify another producer. The next useful step is to reuse existing typed CFG evidence in cross-entry grouping or validation reporting.
Edit targets: `angr_platforms/angr_platforms/X86_16/structuring_graph_builder.py`, `angr_platforms/angr_platforms/X86_16/structuring_cfg_snapshot.py`, `angr_platforms/angr_platforms/X86_16/structuring_cfg_ownership.py`, `angr_platforms/angr_platforms/X86_16/structuring_cfg_grouping.py`, `angr_platforms/angr_platforms/X86_16/structuring_diagnostics.py`, `angr_platforms/angr_platforms/X86_16/decompiler_structuring_stage.py`, `angr_platforms/tests/test_x86_16_structuring_cfg_snapshot.py`, `angr_platforms/tests/test_x86_16_structuring_cfg_ownership.py`, `angr_platforms/tests/test_x86_16_structuring_cfg_grouping.py`, `angr_platforms/tests/test_x86_16_structuring_diagnostics.py`.
Required edits: Pick one consumer path; read snapshot/ownership facts instead of recomputing them; expose the reused facts in diagnostics or grouping output; keep ambiguous multi-entry cases explicit instead of forcing a prettier structure.
Required tests: The targeted structuring CFG snapshot/ownership/grouping tests touched by the chosen consumer; `angr_platforms/tests/test_x86_16_package_exports.py -k structuring`.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_structuring_cfg_snapshot.py angr_platforms/tests/test_x86_16_structuring_cfg_ownership.py angr_platforms/tests/test_x86_16_structuring_cfg_grouping.py angr_platforms/tests/test_x86_16_structuring_diagnostics.py`.
Definition of done: One later consumer reads the typed CFG evidence directly, and no duplicate ownership/grouping logic is introduced.
Stop conditions: Stop if the change starts creating new CFG facts instead of consuming the existing exported ones.

8. Goal: Wire helper/effect routing and first-class recovery artifacts into one external report/cache consumer so corpus runs persist reusable evidence alongside validation artifacts.
Why now: Phase 14 and Phase 15 are both open on the same adoption gap: the repo already emits `FunctionEffectSummary`, helper routing rows, and recovery artifacts, but the current external-facing COD reports still center only on validation summaries.
Edit targets: `angr_platforms/angr_platforms/X86_16/function_effect_summary.py`, `angr_platforms/angr_platforms/X86_16/helper_effect_summary.py`, `angr_platforms/angr_platforms/X86_16/helper_family_routing.py`, `angr_platforms/angr_platforms/X86_16/recovery_confidence.py`, `angr_platforms/angr_platforms/X86_16/recovery_artifacts.py`, `angr_platforms/angr_platforms/X86_16/recovery_artifact_writer.py`, `angr_platforms/angr_platforms/X86_16/targeted_recovery_artifact.py`, `angr_platforms/angr_platforms/X86_16/corpus_recovery_artifact.py`, `scripts/write_recovery_artifact.py`, `scripts/write_recovery_artifact_batch.py`, `scripts/decompile_cod_dir.py`, `angr_platforms/angr_platforms/X86_16/validation_manifest.py`.
Required edits: Choose one consumer surface outside `recovery_confidence`; attach helper-routing and recovery-artifact paths/keys to that surface; keep the serialized schema stable and deterministic; do not add new logic to `corpus_scan.py` or weaken tail-validation accounting.
Required tests: `angr_platforms/tests/test_x86_16_function_effect_summary.py`; `angr_platforms/tests/test_x86_16_helper_effect_summary.py`; `angr_platforms/tests/test_x86_16_recovery_confidence_helper_summary.py`; `angr_platforms/tests/test_x86_16_targeted_recovery_artifact.py`; the smallest report/cache test affected by the chosen consumer.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_function_effect_summary.py angr_platforms/tests/test_x86_16_helper_effect_summary.py angr_platforms/tests/test_x86_16_recovery_confidence_helper_summary.py angr_platforms/tests/test_x86_16_targeted_recovery_artifact.py`; run the chosen writer script on one bounded COD target and inspect the emitted JSON.
Definition of done: One external report or cache descriptor persists helper-routing and recovery-artifact references deterministically alongside validation artifacts.
Stop conditions: Stop if the only way to expose the data is to duplicate existing artifact payloads or to special-case one corpus sample.

9. Goal: Re-run the full recompilable-subset smoke and keep corpus robustness evidence current after the targeted correctness fixes land.
Why now: Recompilation has no fresh evidence in `.codex_automation/evidence.log`, and the prior plan still marks the full heavy recompilable-subset smoke as outstanding; correctness fixes must be checked against both the COD harness and the recompilable backend.
Edit targets: `angr_platforms/angr_platforms/X86_16/recompilable_subset.py`, `angr_platforms/angr_platforms/X86_16/recompilable_cases.py`, `angr_platforms/angr_platforms/X86_16/recompilable_checks.py`, `angr_platforms/angr_platforms/X86_16/recompilable_cli_bridge.py`, `angr_platforms/angr_platforms/X86_16/recompilable_storage_map.py`, `angr_platforms/angr_platforms/X86_16/recompilable_storage_map_producer.py`, `angr_platforms/angr_platforms/X86_16/recompilable_storage_alias.py`, `.codex_automation/evidence.log`, `PLAN.md`.
Required edits: After the targeted correctness items are green, run the bounded recompilable smoke; record pass/fail plus command, runtime, and any failing cases in `.codex_automation/evidence.log`; if the smoke fails, create the next smallest focused item from the failing case and keep this item open.
Required tests: `angr_platforms/tests/test_x86_16_recompilable_subset.py`; `angr_platforms/tests/test_x86_16_smoketest.py`; any narrower recompilable test that matches the observed failure.
Verification commands: `.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_recompilable_subset.py angr_platforms/tests/test_x86_16_smoketest.py`; run the repo’s bounded recompilable-subset smoke command once and append the result to `.codex_automation/evidence.log`.
Definition of done: Current evidence contains one fresh recompilable-subset smoke result tied to the same code state as the latest correctness fixes.
Stop conditions: Stop if correctness-focused edits are still moving and would invalidate the recompilation run; do not churn this smoke repeatedly on unchanged code.
