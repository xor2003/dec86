See AGENTS.md and make sure you apply fixes to the rigth architectural layer.

3. Goal: Capture one bounded stage bundle on first repeated exact-lane family so timeout proof stops being low-confidence.
Why now: `.codex_automation/evidence_facts.json` marks timeout and fallback facts low-confidence. The one-function logs show the family, but not raw asm, COD window, raw codegen, post-callsite codegen, post-stack-lowering codegen, or final stderr/stdout for the first repeated family.
Edit targets: `/home/xor/vextest/inertia_decompiler/direct_addr_stage_bundle.py`, `/home/xor/vextest/inertia_decompiler/cli_decompilation.py:700-820`, `/home/xor/vextest/inertia_decompiler/cli_core.py:1741-1861`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_sortdemo_regressions.py:230-265`.
Required edits: Add one small helper module that writes deterministic files under `.codex_automation/stage_debug/` keyed by binary path, function start, and family key. Hook it only on the first repeated-family stop for exact-address runs. Save raw asm, COD window, raw codegen text, post-callsite text, post-stack-lowering text, and final stdout/stderr summary. Print the bundle dir path once.
Required tests: `angr_platforms/tests/test_x86_16_cli.py -k "exact_lane or timeout or validation" -q`; `angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "quicksort or runmenu" -q`.
Verification commands: `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_cli.py angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "exact_lane or timeout or validation or quicksort or runmenu" -q`; `./.venv/bin/python -u decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x102e0 --timeout 30 --trace-c-stages`; `./.venv/bin/python -u decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10ce0 --timeout 30 --trace-c-stages`.
Definition of done: First repeated RunMenu or QuickSort family writes one deterministic stage bundle with all required files, and later same-family stops reuse it.
Stop conditions: Stop if capture changes timeout budget or decompiler semantics. Stop if file names are not deterministic.
Estimated rounds: 2.

4. Goal: Push stable SS and DS storage proof earlier so HeapSort and PercolateUp lose avoidable segmented ladders in emitted C.
Why now: `.codex_automation/evidence.log` still shows HeapSort and PercolateUp with `((ss << 4) + ...)` carriers and raw DS byte-pair/global updates. Current correctness is partial. Recompile shape is still weak.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/widening/store_width.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/widening/widening_rules.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/lowering/stack_lowering.py:18-47`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/lowering/segmented_lowering.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/lowering/object_lowering.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_cod_regressions.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_segmented_memory.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_storage_objects.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_sortdemo_regressions.py:230-265`.
Required edits: Preserve SS word-store width only when segment, range, and alias identity agree. Feed that proof into segmented and object lowering so stable SS locals and stable DS globals lower earlier, while mixed-segment or weak-proof cases stay explicit. Add negative tests for cross-segment byte pairs and over-associated DS objects.
Required tests: `angr_platforms/tests/test_x86_16_cli.py -k "coalesce_direct_ss_local_word_statements" -q`; `angr_platforms/tests/test_x86_16_cod_regressions.py -k "word_store or byte_pair or stack" -q`; `angr_platforms/tests/test_x86_16_segmented_memory.py -q`; `angr_platforms/tests/test_x86_16_storage_objects.py -q`; `angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "main or HeapSort or PercolateUp" -q`.
Verification commands: `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_cli.py -k "coalesce_direct_ss_local_word_statements" -q`; `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_cod_regressions.py -k "word_store or byte_pair or stack" -q`; `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_segmented_memory.py angr_platforms/tests/test_x86_16_storage_objects.py -q`; `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "main or HeapSort or PercolateUp" -q`; `./.venv/bin/python -u decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10970 --timeout 30 --alternate-source-c`; `./.venv/bin/python -u decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x109e8 --timeout 30 --alternate-source-c`.
Definition of done: HeapSort and PercolateUp each lose at least one avoidable raw SS or DS ladder where alias proof is stable, and ambiguous segment cases stay explicit.
Stop conditions: Stop if the join uses shape only. Stop if lowering flattens unknown segment identity into globals.
Estimated rounds: 3.

5. Goal: Normalize mixed-width typed conditions before structuring so Sleep stays clean and QuickSort stops leaning on raw flag shape.
Why now: `.codex_automation/evidence.log` still shows Sleep as only partly clean, and QuickSort still sits in the structuring-failure family from `.codex_automation/onefn/0x10ce0.log`. This is early IR work, not late rewrite work.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/ir/condition_ir.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/ir/vex_condition_lifting.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/semantics/alu_semantics.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_condition_ir.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_alu_helpers.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_sortdemo_regressions.py:65-96`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_sortdemo_regressions.py:153-180`.
Required edits: Carry compare width and flag meaning into typed `Condition` objects before structuring. Fold zero/carry consumers from structured IR, not from rendered text. Use item 3 stage bundles before editing if QuickSort proof is still thin.
Required tests: `angr_platforms/tests/test_x86_16_condition_ir.py -q`; `angr_platforms/tests/test_x86_16_alu_helpers.py -q`; `angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "sleep or quicksort" -q`.
Verification commands: `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_condition_ir.py angr_platforms/tests/test_x86_16_alu_helpers.py -q`; `./.venv/bin/pytest -q angr_platforms/tests/test_x86_16_sortdemo_regressions.py -k "sleep or quicksort" -q`; `./.venv/bin/python -u decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f38 --timeout 30 --alternate-source-c`; `./.venv/bin/python -u decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10ce0 --timeout 30 --alternate-source-c`.
Definition of done: Sleep keeps stable validation with clearer compare output, and QuickSort either decompiles with typed-condition gains or stops with one explicit family plus stage bundle.
Stop conditions: Stop if the only path is late text cleanup. Stop if QuickSort still lacks stage-bundle proof.
Estimated rounds: 3.

6. Goal: Remove alias compatibility shims and lock one canonical x86-16 stage order after live SORTDEMO retry work lands.
Why now: Retry gating and typed lowering stay brittle while alias ownership and stage order stay split. This is still open, but it must trail the live SORTDEMO lane.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/alias_model.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/alias_domains.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/alias/alias_model.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/alias/domains.py`, `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/inertia_decompiler/cli_decompilation.py:821-915`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_structuring_stage.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`, `/home/xor/vextest/tools/check_import_layers.py`, `/home/xor/vextest/angr_platforms/tests/test_pipeline_order.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_layer_boundaries.py`.
Required edits: Move callers to canonical alias modules. Delete dynamic re-export glue. Add one import smoke test and one pipeline-order test that fixes the order for callsite facts, widening, lowering, structuring, postprocess, and validation. Keep big files to thin hook edits only.
Required tests: `angr_platforms/tests/test_pipeline_order.py -q`; `angr_platforms/tests/test_x86_16_layer_boundaries.py -q`.
Verification commands: `rg -n "globals\\(\\)\\.update|import \\*" angr_platforms/angr_platforms/X86_16`; `PYTHONPATH=/home/xor/vextest/angr_platforms ./.venv/bin/python -c "import angr_platforms.X86_16.alias.alias_model; import angr_platforms.X86_16.alias.domains"`; `./.venv/bin/pytest -q angr_platforms/tests/test_pipeline_order.py angr_platforms/tests/test_x86_16_layer_boundaries.py -q`; `./.venv/bin/python tools/check_import_layers.py`.
Definition of done: Alias shims are gone, reverse-import guard tests pass, and the focused SORTDEMO path still runs in one stage order.
Stop conditions: Stop if this turns into a broad CLI rewrite before items 1 through 5 move live SORTDEMO quality. Stop if caller mapping is still unclear.
Estimated rounds: 3.
