1. Goal: Add typed low-memory read/write facts to function-state artifacts, with raw-vs-classified provenance kept side by side.
Why now: `angr_platforms/tests/test_x86_16_function_interface_surface.py:56-86` proves low-memory naming already shows in interface comments, but `angr_platforms/angr_platforms/X86_16/function_state_summary.py:15-147` still exports only raw `memory_reads` and `memory_writes`, so packet 1 is blocked above the typed artifact layer.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/function_state_summary.py:15-147`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/low_memory_regions.py:14-156`; one new helper under `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/` if the summary dataclasses would push `function_state_summary.py` past repo size rules; `/home/xor/vextest/angr_platforms/tests/test_x86_16_function_state_summary.py`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_low_memory_regions.py`.
Required edits: define a typed low-memory access row/dataclass with fields for access kind, raw access text, classified region, classified label, linear address, segment, offset, and size; add deterministic collectors that classify `memory_reads` and `memory_writes` through `classify_x86_16_low_memory_access`; store both raw tuples and classified tuples on `FunctionStateSummary`; keep unknown accesses out of the classified tuples without deleting them from raw memory effects; expose the new tuples from `brief()` and `to_dict()` only in stable sorted order.
Required tests: update `test_function_state_summary_partitions_gp_segment_and_flags`; add one state-summary regression for mixed raw/classified reads+writes; add one low-memory-regions regression that proves exact-field hits stay exact and region-only hits stay region-only.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_function_state_summary.py angr_platforms/tests/test_x86_16_low_memory_regions.py`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/scripts/run_plan3_checks.py --no-py-compile`
Definition of done: `summarize_x86_16_function_state()` returns typed low-memory rows for exact BDA/IVT/video/ROM hits, keeps raw memory tuples unchanged, and serializes both surfaces deterministically.
Stop conditions: stop if the only available source is rendered comment text instead of structured `memory_reads`/`memory_writes`; stop if the file would exceed 400 lines without first splitting the helper into a new module.
Status: complete after typed low-memory rows landed and both focused checks passed.

2. Goal: Thread typed low-memory state into per-function and corpus recovery artifacts.
Why now: `angr_platforms/angr_platforms/X86_16/recovery_artifacts.py:28-129` and `angr_platforms/tests/test_x86_16_recovery_artifacts.py:7-103` currently stop at generic `state_summary`, so packet 1 still has no recovery-artifact lane for named BDA/IVT/video/ROM access.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/recovery_artifacts.py:28-129`; `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/recovery_artifact_writer.py:22-60`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_recovery_artifacts.py`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_recovery_artifact_writer.py`.
Required edits: extend `FunctionRecoveryArtifact.to_dict()` coverage so the new low-memory state survives artifact writes; add deterministic corpus-level aggregate counters for low-memory read regions and write regions inside `CorpusRecoveryArtifact`; keep aggregate counters derived from typed rows, not from text parsing; update writer expectations only through the artifact objects, not with ad hoc JSON assembly.
Required tests: extend `test_function_recovery_artifact_embeds_effect_helper_and_confidence`; extend `test_corpus_recovery_artifact_keeps_helper_family_rows_deterministic`; extend writer coverage in `test_x86_16_recovery_artifact_writer.py` so serialized JSON includes the new function-row and corpus aggregate fields.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_recovery_artifacts.py angr_platforms/tests/test_x86_16_recovery_artifact_writer.py`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/scripts/run_plan3_checks.py --no-py-compile`
Definition of done: function recovery artifact JSON carries typed low-memory rows, and corpus recovery artifact JSON carries deterministic low-memory aggregate counts without losing existing helper/confidence fields.
Stop conditions: stop if the change would require scanning rendered C or comment text; stop if aggregate rows cannot be derived from `FunctionStateSummary` alone and a new earlier typed source must be added first.

3. Goal: Expose low-memory classification in the bounded COD corpus recovery summary path, not just in raw JSON rows.
Why now: packet 1 says “typed recovery artifacts and corpus summaries”; `angr_platforms/angr_platforms/X86_16/corpus_recovery_artifact.py:15-95` currently reports only proc count plus confidence/helper counts, so the corpus-facing summary still hides low-memory evidence.
Edit targets: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/corpus_recovery_artifact.py:15-95`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_corpus_recovery_artifact.py`; if needed, `/home/xor/vextest/angr_platforms/tests/test_x86_16_recovery_artifacts.py`.
Required edits: add bounded summary fields for low-memory read-region counts, low-memory write-region counts, and optional top labels to `CorpusCodRecoveryArtifactResult`; source those fields from the already-built corpus recovery artifact instead of re-classifying inputs; keep ordering deterministic and counts conservative; avoid folding low-memory facts into helper/confidence counts.
Required tests: extend `test_corpus_recovery_artifact_writes_bounded_cod_batch`; add one synthetic COD batch case that exercises both a read and a write low-memory label in the returned summary dict.
Verification commands: `/home/xor/vextest/.venv/bin/python -m pytest angr_platforms/tests/test_x86_16_corpus_recovery_artifact.py angr_platforms/tests/test_x86_16_recovery_artifacts.py`; `/home/xor/vextest/.venv/bin/python /home/xor/vextest/scripts/run_plan3_checks.py --no-py-compile`
Definition of done: the bounded COD corpus recovery result exposes deterministic low-memory summary counts built from typed artifact data, and the JSON artifact plus returned summary agree.
Stop conditions: stop if the only path to these counts duplicates classification logic outside the typed artifact layer; stop if adding the fields would break existing artifact shape without a clear compatibility update in tests.
1. [blocked] Finish the honest full-COD tail-validation sweep on `/home/xor/vextest/cod`. Target files: `/home/xor/vextest/scripts/decompile_cod_dir.py`, `/home/xor/vextest/angr_platforms/tests/test_decompile_cod_dir_parallelism.py`, `/home/xor/vextest/PLAN2.md`. Done so far: bounded child-timeout policy, `uncollected` placeholder machinery, and an explicit `sys.stdout.flush()` before wrapper tail-validation emission now exist, with coverage in `/home/xor/vextest/angr_platforms/tests/test_decompile_cod_dir_parallelism.py` and `/home/xor/vextest/angr_platforms/tests/test_x86_16_corpus_scan.py`. Fresh honest artifact: `/home/xor/vextest/angr_platforms/.cache/tail_validation_details/cod-c7d08c1cd3a5.timeout3.tail_validation_surface.json`. Current blocker wording: the fresh no-skip artifact now lands after the final `[432/432]` batch output, but it still reports `severity=changed` with 5 changed functions in the `structuring` `control-flow/guard delta` family (`CARR.COD:_SetGear`, `EGAME11.COD:_strcpyFromDot`, `OUTPUT.COD:_hexdump`, `PLANES3.COD:_CheckIfCanIntercept`, `START1.COD:_waitMdaCgaStatus`) plus 259 uncollected functions. Next cycle should stay on that named 5-function structuring family before another broad sweep. Definition of done: there is one reproducible no-skip full-COD artifact and console summary for the whole corpus, not a partial or sample-only rerun.
2. [pending] Verify that corpus aggregate now counts timeout/fallback PROC results as `uncollected` instead of dropping them. Target files: `/home/xor/vextest/scripts/decompile_cod_dir.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py`, `/home/xor/vextest/angr_platforms/tests/test_decompile_cod_dir_parallelism.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_corpus_scan.py`, `/home/xor/vextest/PLAN2.md`. Done so far: aggregate/reporting support for `uncollected` identities already exists in `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py`. Remaining work: confirm it survives the final full-corpus run and that the resulting artifact names `uncollected` PROC rows by `cod_file/proc_name/proc_kind`, not just by counts. Definition of done: the final full corpus report can name which PROC results are `uncollected`, not just how many changed.
3. [done] Fix family A: DS global/segmented-write alias mismatch in postprocess. Target files: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation_fingerprint.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_stage.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_tail_validation.py`, `/home/xor/vextest/PLAN2.md`. Done so far: collector-side normalization now treats `deref:ds:*` and `global:*` as equivalent only when segmented-memory reasoning has already proven `DS` linear lowering safe, `/home/xor/vextest/angr_platforms/tests/test_x86_16_tail_validation.py` contains the focused `DoCRT` write regression plus the distinctness guard, and the affected COD subset (`COCKPIT.COD:_DoCRT`, `NHORZ.COD:_DrawAAA`, `PLANES3.COD:_DoOClockCall`, `PLANES3.COD:_Ready5`, `PLANES3.COD:_SARWait`) now reruns clean with `whole-tail validation clean across 5 functions`. Definition of done: satisfied.
4. [done] Fix family B: `_DisplayMaster` postprocess condition/control-flow delta. Target files: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/tail_validation_condition_context.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/decompiler_postprocess_flags.py`, `/home/xor/vextest/angr_platforms/tests/test_x86_16_tail_validation.py`, `/home/xor/vextest/PLAN2.md`. Done so far: collector-side contextual condition fingerprinting now collapses adjacent flag-assignment/guard pairs to the recovered predicate, `test_tail_validation_normalizes_adjacent_flag_assignment_guard_pairs` passes, and `_DisplayMaster` reruns clean with `/home/xor/vextest/.venv/bin/python /home/xor/vextest/scripts/decompile_cod_dir.py /home/xor/vextest/cod --cod-file f14/COCKPIT.COD --proc-name _DisplayMaster --timeout 10 --max-workers 1`. Definition of done: satisfied; the remaining timeout-3 behavior is a timeout-budget issue, not the control-flow family.
5. [in_progress] Re-run the whole COD corpus after each family fix until only an explicit named exception list remains, or the corpus is fully clean. Target files: `/home/xor/vextest/scripts/decompile_cod_dir.py`, `/home/xor/vextest/angr_platforms/.cache/tail_validation_details/`, `/home/xor/vextest/PLAN2.md`. Done so far: `/home/xor/vextest/.codex_automation/evidence.log:2221-2236` already names the current residue as `postprocess` `control-flow/guard delta` (8 functions, including `CARR.COD:_SetGear`, `PLANES3.COD:_CloseEnoughFor`, `PLANES3.COD:_GetPlaneOrgNum`) plus 3 named `3DLOADER.COD` `subprocess_timeout` rows, and the harness now emits immutable content-addressed detail artifacts so later runs cannot overwrite the evidence path cited by a prior sweep. Remaining work: rerun the full no-skip corpus sweep on this local diff, refresh the detail artifact, and shrink the remaining `changed/unknown/uncollected` set; if any residue remains, name it explicitly and back it with tests instead of leaving it ambient. Definition of done: full COD tail-validation is either clean or reduced to a tiny explicit tested exception list backed by immutable current artifacts.
6. [pending] Propagate low-memory classification from output comments into typed recovery artifacts and corpus summaries. Target files: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/recovery_artifacts.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/recovery_confidence.py`, one new small helper under `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/`, focused tests under `/home/xor/vextest/angr_platforms/tests/`, `/home/xor/vextest/PLAN2.md`. Required edits: add a typed low-memory artifact with deterministic ordering and raw-vs-classified provenance; normalize equivalent real-mode aliases by linear address before classification (`0x40:0x2 == 0x0:0x402 == 0x3f:0x12 == linear 0x402`); store classified BDA/IVT/video/ROM accesses in function recovery artifacts and corpus summaries; keep raw memory effects alongside classified facts. Definition of done: one typed artifact surface exposes named low-memory accesses with deterministic provenance and corpus-level region counts, and focused tests prove alias-equivalent segment:offset forms classify identically.
7. [pending] Use typed low-memory facts in one conservative semantic consumer. Target files: one bounded consumer under `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/` plus focused tests and `/home/xor/vextest/PLAN2.md`. Required edits: make one existing pass change bounded behavior from typed low-memory facts rather than raw address strings, for example by surfacing BIOS-state access explicitly or excluding low-memory regions from generic object/array guessing. Definition of done: one non-output pass changes behavior from typed low-memory facts.
8. [pending] Extend low-memory classification to bounded dynamic `0x40:xx`-style accesses. Target files: `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/low_memory_regions.py`, one small typed-IR or segment-state helper, focused tests under `/home/xor/vextest/angr_platforms/tests/`, `/home/xor/vextest/PLAN2.md`. Required edits: support region-level classification for provable bounded dynamic accesses without inventing exact field names; classify from normalized linear ranges rather than preserving one chosen segment spelling; keep exact field naming only for exact address+width matches. Definition of done: exact-field, region-only, unresolved dynamic cases, and alias-equivalent segment:offset forms are all covered by focused tests.
9. [pending] Add one real corpus anchor that emits named low-memory/BDA access through the normal output path. Target files: one stable corpus sample under `/home/xor/vextest/cod/`, one focused anchor test under `/home/xor/vextest/angr_platforms/tests/`, and `/home/xor/vextest/PLAN2.md`. Current candidate: `BIOSFUNC.COD::_bios_clearkeyflags`, because it writes the BDA keyboard flags and already has a stable regression harness. Required edits: route typed low-memory evidence into the emitted decompiled surface without sample-specific text hacks; accept any equivalent segment:offset spelling as long as it resolves to the same named linear location. Definition of done: one real emitted output contains a named low-memory state such as `bda.keyboard_flags0`, not only anonymous `0x40:xx` or linear address trivia.
10. [pending] Add typed PSP (Program Segment Prefix) classification as a separate DOS region model, not as generic low memory. Target files: one new small helper under `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/`, one bounded consumer under `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/`, focused tests under `/home/xor/vextest/angr_platforms/tests/`, and `/home/xor/vextest/PLAN2.md`. Why separate: PSP is process-relative and segment-based, unlike fixed physical ranges such as BDA/IVT/video RAM. Required edits: model exact PSP fields (for example `psp.int22_terminate`, `psp.int23_ctrl_break`, `psp.environment_segment`, `psp.command_tail_len`, `psp.command_tail`, `psp.default_fcb1`, `psp.default_fcb2`) from `segment:offset` when the PSP segment is known or proven; normalize equivalent segment:offset aliases by linear address inside the proven PSP segment; keep region-only `psp+0xNN` naming for bounded dynamic offsets; do not flatten PSP into generic DS memory. Definition of done: one typed artifact and one emitted-output or semantic consumer can name PSP fields conservatively, with focused tests for exact-field, region-only, unresolved cases, and alias-equivalent segment spellings.
1. Goal: Stop `SORTDEMO` direct-function fallback from re-running both bounded fallback families after the same dead `decompile:empty` result.
Why now: `/tmp/sortdemo.perc.addr.out` and `/tmp/sortdemo.sleep.addr.out` still show five primary/fallback decompiles per function even after bounded-lane verdict pruning, because `_try_decompile_sidecar_slice()` at `/home/xor/vextest/inertia_decompiler/cli.py:949` and `_try_decompile_non_optimized_slice()` at `/home/xor/vextest/inertia_decompiler/cli.py:1046` do not share dead-lane verdicts. Current quality: correctness red, recompilation red.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py:949`; `/home/xor/vextest/inertia_decompiler/cli.py:1046`; `/home/xor/vextest/inertia_decompiler/cli.py:13332`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`; `/home/xor/vextest/angr_platforms/tests/test_slice_recovery_verdicts.py`.
Required edits: Add one typed handoff from sidecar-slice fallback to non-optimized fallback so a repeated `stage=decompile, stop_family=empty` on the same bounded region closes the second family instead of replaying it. Keep the first family-specific failure detail visible. Do not use rendered asm/C text to classify the dead family.
Required tests: Keep `test_try_decompile_non_optimized_slice_retries_after_bounded_lean_timeout`. Add one focused direct-function test proving `SORTDEMO`-style empty sidecar bounded failure skips the later non-optimized family, and one focused test proving a non-empty sidecar result still allows the later family when the stop family changes.
Verification commands: `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python -m pytest -q angr_platforms/tests/test_slice_recovery_verdicts.py angr_platforms/tests/test_x86_16_cli.py -k "bounded_lean_timeout or sidecar_slice or non_optimized_slice"`; `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x109d8 --timeout 6 > /tmp/sortdemo.perc.addr.out 2>&1`; `rg -c "Decompiler returned for 0x109d8" /tmp/sortdemo.perc.addr.out`.
Definition of done: `SORTDEMO.EXE --addr 0x109d8 --timeout 6` and `--addr 0x10f28` no longer replay both bounded fallback families after the same typed dead-empty result, and the direct output explains which family was pruned.
Stop conditions: Stop if the only available signal for cross-family dedupe is rendered asm/C text instead of typed attempt outcomes.

2. Goal: Make unresolved `SORTDEMO` empties actionable by surfacing the failing pass/block family instead of only `IR-shaped C`.
Why now: `Sleep` and `PercolateUp` still fall to asm even with helper metadata, and the current visible failure text in `_emit_function_result()` at `/home/xor/vextest/inertia_decompiler/cli.py:13332` hides the exact clinic/block family already seen in debug output. Current quality: correctness red, recompilation red.
Edit targets: `/home/xor/vextest/inertia_decompiler/cli.py:13332`; `/home/xor/vextest/inertia_decompiler/decompilation_quality.py`; `/home/xor/vextest/angr_platforms/tests/test_x86_16_cli.py`.
Required edits: Capture the first stable structured failure hint for unresolved C in the direct function path and print it with the empty result. Prefer typed or existing debug-stage artifacts from the current run; do not scrape rendered asm or final C text. Keep the normal success path unchanged.
Required tests: Add one focused test that an unresolved empty result with a captured clinic/block hint prints that hint before asm fallback, and keep the existing unresolved-empty fallback tests passing.
Verification commands: `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python -m pytest -q angr_platforms/tests/test_x86_16_cli.py -k "empty or asm fallback or clinic"`; `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --addr 0x10f28 --timeout 6 > /tmp/sortdemo.sleep.addr.out 2>&1`; `rg -n "clinic|empty|asm fallback" /tmp/sortdemo.sleep.addr.out`.
Definition of done: A direct `SORTDEMO` empty result names the current failing pass/block family in the emitted output, so the next fix can target a real semantic layer instead of a generic asm fallback.
Stop conditions: Stop if no stable current-run hint exists outside raw rendered asm/C text; in that case add the missing typed hint first instead of inventing one.

3. Goal: Turn at least one current `SORTDEMO` asm fallback (`Sleep` or `PercolateUp`) into honest recovered C through the earliest failing semantic layer.
Why now: `/tmp/sortdemo.current.out` still falls back to asm for `Sleep`, `PercolateUp`, and one additional shown function after the repetition fix. The remaining blockers are no longer control-flow retries; they are real recovery gaps. Current quality: correctness red, recompilation red.
Edit targets: The earliest layer localized by item 2; expected candidates are `/home/xor/vextest/inertia_decompiler/cli.py`, `/home/xor/vextest/angr_platforms/angr_platforms/X86_16/*`, and a new focused regression test file if the touched layer is outside existing coverage.
Required edits: Use the new failure-family signal to identify the first incorrect layer for one current `SORTDEMO` asm fallback and fix it there. Keep the change generic for x86-16 real-mode code; do not special-case `SORTDEMO`, symbol names, or addresses.
Required tests: Add one focused regression for the chosen function family and keep the bounded-fallback tests from items 1 and 2 passing.
Verification commands: `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python -m pytest -q <focused test names>`; `PYTHONPATH=angr_platforms /home/xor/vextest/.venv/bin/python /home/xor/vextest/decompile.py /home/xor/vextest/SORTDEMO.EXE --timeout 6 --max-functions 8 > /tmp/sortdemo.current.out 2>&1`; `python - <<'PY'\nfrom pathlib import Path\ntext=Path('/tmp/sortdemo.current.out').read_text(errors='replace')\nprint('asm_fallback', text.count('asm fallback'))\nprint('decompiler_returned', text.count('Decompiler returned'))\nPY`.
Definition of done: The bounded `SORTDEMO` sweep reduces current asm fallback count by at least one without increasing repeated decompile attempts for the shown functions.
Stop conditions: Stop if the only plausible “fix” is source-sidecar substitution, symbol-name heuristics, or more timeout budget.
