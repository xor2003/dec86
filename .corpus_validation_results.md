# Corpus Validation Results: Items 3-7

**Date**: 2026-04-05  
**Status**: Partial Completion  
**Run Time**: ~10 minutes total

---

## Summary: Item Status

| Item | Component | Target File | Status | Evidence |
|------|-----------|-----------|--------|----------|
| **5** | BYTEOPS byte-register | `cod/default/BYTEOPS.COD` | ✅ **PASS** | All byte ops render as `a`, `b` not `ax_`, `bx_` |
| **6** | OVERLAY object recovery | `cod/OVERLAY.COD` | ✅ **PASS** | 3/3 segmented-space tests pass |
| **7** | DOSFUNC helpers | `cod/DOSFUNC.COD` | ✅ **PASS** | 3/3 helper signature tests pass |
| **3** | Call-heavy classifier | `corpus_scan.py` | ❌ **FAIL** | 4/28 tests failing (needs implementation) |
| **4** | Tiny-helper CFG bypass | `corpus_scan.py` | ❌ **FAIL** | 4/28 tests failing (needs implementation) |

---

## Item 5: BYTEOPS Byte-Register Correctness ✅ **COMPLETE**

**Test**: `test_byteops_cod_main_renders_named_byte_locals_without_generic_staging_names`  
**Result**: **PASS** ✅

**Verification**:
```
✓ a = 255;
✓ b = 143;
✓ b = a + b;
✓ a = a - b;
✓ a = a * b;
✓ b = b / a;
✓ b = b % a;
✓ a = a << 5;
✓ b = b >> a;
✓ printf("a = %d, b = %d\\n", a, b);
✗ NO synthetic ax_ or bx_ variables
```

**Architecture Layer**: Alias/Widening Model  
**Confidence**: HIGH (evidence-driven byte-register identity working correctly)

---

## Item 6: OVERLAY Segmented-Space Object Recovery ✅ **COMPLETE**

**Tests**:
1. `test_overlay_cod_sample_rewrites_far_pointer_stack_pair_to_mk_fp` → **PASS** ✅
2. `test_overlay_cod_sample_wrapper_returns_overlay_segment` → **PASS** ✅
3. `test_cod_block_lift_cases[f14_overlay_loader_block]` → **PASS** ✅

**Verification**:
```
✓ Far pointers rendered as MK_FP(segment, offset)
✓ Segmented memory space correctly distinguished (DS vs ES)
✓ Stack pair merging respects segment boundaries
```

**Architecture Layer**: Segmented Memory Association  
**Confidence**: HIGH (cross-segment tracking stable)

---

## Item 7: DOSFUNC Helper Recovery ✅ **COMPLETE**

**Tests**:
1. `test_dosfunc_cod_sample_deduplicates_stack_local_names` → **PASS** ✅
2. `test_dosfunc_cod_sample_process_helpers_stay_empty[_dos_getProcessId]` → **PASS** ✅
3. `test_dosfunc_cod_sample_process_helpers_stay_empty[_dos_setProcessId]` → **PASS** ✅

**Verification**:
```
✓ Process helpers return correct types (unsigned short, int)
✓ Helper signatures source-backed (no synthetic invocations)
✓ Stack locals properly declared and deduplicated
```

**Architecture Layer**: Helper Recovery + Source Annotation  
**Confidence**: HIGH (source-backed signatures stable)

---

## Items 3-4: Scan-Safe Classifiers ⚠️ **PARTIAL**

**Test Suite**: `test_x86_16_corpus_scan.py`  
**Results**: 24/28 passing, 4 failing

### Failures:

1. **`test_scan_safe_keeps_short_dos_memory_helpers_in_bounded_recovery[_dos_mcbInfo]`**
   - Expected: `fallback_kind in {"cfg_only", "block_lift"}`
   - Got: `lift_only`
   - Context: Small DOS memory helper, should use CFG shortcut

2. **`test_scan_safe_keeps_known_hotspots_in_conservative_recovery[OUTPUT.COD-_hexdump]`**
   - Expected: `cfg_only` (known short helper)
   - Got: `lift_only`
   - Context: Hotspot helper, should recognize short pattern

3. **`test_scan_safe_keeps_known_hotspots_in_conservative_recovery[START1.COD-_processStoreInput]`**
   - Expected: `cfg_only` (call-heavy pattern)
   - Got: `lift_only`
   - Context: Input processing function, should classify by call density

4. **`test_scan_safe_keeps_known_hotspots_in_conservative_recovery[START3.COD-_sub_14BB4]`**
   - Expected: `cfg_only` or `block_lift`
   - Got: `lift_only`
   - Context: Addressing helper, should use conservative path

### Root Cause:

The classifiers that route functions to `cfg_only` (tiny-helper bypass) or `block_lift` (call-heavy classification) **are not yet implemented**. Currently all oversized functions fall back to `lift_only` (VEX-only, conservative recovery).

### Required Implementation (PLAN.md Items 3-4):

**Item 3: Call-Heavy Helper Classifier**
- Detect: Functions with many direct calls (>N% of instructions)
- Route to: `cfg_only` (conservative stack-control analysis)
- Expected behavior: Classify without full CFG overhead

**Item 4: Tiny-Helper CFG Bypass**
- Detect: Single-call + early-return pattern
- Route to: Skip full CFG, emit conservative block lift
- Expected behavior: Avoid CFG timeout on micro-helpers

---

## Architecture Check

**All phases maintain core properties**:
- ✅ Alias-first reasoning (byte-register identity proves storage correctness)
- ✅ Evidence-driven claims (NO synthetic `ax_` guessing in BYTEOPS)
- ✅ Correctness-first (ugly `lift_only` is honest, not pretty)
- ✅ Conservative segmented memory (DS/ES distinct, no flattening)

---

## Next Steps

### Immediate (This Session):

1. **Implement Item 3**: Call-heavy helper classifier in `corpus_scan.py`
   - Add bytecode pattern matcher for direct calls
   - Route to `cfg_only` lane

2. **Implement Item 4**: Tiny-helper CFG bypass
   - Add shape matcher for call + return pattern
   - Skip CFG, return `block_lift` fallback

### After Implementation:

- Re-run `test_x86_16_corpus_scan.py` (expect 28/28 passing)
- Verify F14 corpus timeouts reduce
- Measure peak RSS before/after

---

## Validation Metrics

| Metric | Item 5 | Item 6 | Item 7 | Items 3-4 |
|--------|--------|--------|--------|-----------|
| **Tests Passing** | 1/1 | 3/3 | 3/3 | 24/28 |
| **Confidence** | HIGH | HIGH | HIGH | MEDIUM (awaiting classifiers) |
| **Correctness** | ✅ | ✅ | ✅ | ⚠️ (conservative but honest) |
| **Architecture** | Alias | Segmented | Source-backed | Scan-safe |

---

## Conclusion

**Three major corpus components validated and working correctly**: BYTEOPS, OVERLAY, DOSFUNC.

**Four corpus scan tests await scan-safe classifier implementations** (Items 3-4).

**All evidence-driven recovery working as designed** - no shortcuts, no guessing, confidence levels guide decisions.

Ready to implement Items 3-4 classifiers for full corpus validation.
